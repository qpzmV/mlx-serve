# 2026-09-01

## expert-streaming-handoff.md 第二轮 review（用户要求）

文档已迁至 `/Users/robin/workspace/1@inference_engine/expert-streaming-handoff.md`。本轮按真实检查点复核，修掉 4 处缺陷：

1. **group_size 写错**：文档写 64，实测 root `quantization` = **32**（`text_config` 无 quantization 键；mlx-serve 从 root 解析，model.zig:1656，所以 quant_group_size 取到 32 是对的）。分片形状 scales `[512,640,80]`（h/g=32）、`[512,2560,20]`（ff/g=32）坐实。
2. **§3.2 池形状数值是 gs64 的**：`[n,640,40]`→`[n,640,80]`、`[n,2560,40]`→`[n,2560,20]`。照抄会导致池形状错。
3. **验收标准编号重复**（我上一轮补哨兵时插入的 4 号与原有 4 号撞号）→ 已重排为 4/5/6。
4. **新增 MTP 专家层发现（重要）**：`switch_mlp` 实际 **49 套**而非 48——主干 `model.layers.{0..47}`（75.50GB）+ 独立分片 `model-00023-of-00023.safetensors`（1.5GB）里的 `language_model.mtp.layers.0.mlp.switch_mlp.*`（1.57GB）。这就是文档 §1 的 75.5GB 与 §0/§2.2 的 77.07GB 差额来源。分片命名是「22 个 of-00022 + 1 个 of-00023」两套并存。

- 新增踩坑 14：几何常量别照抄 slotstream（`Plan.swift:15-31` 的 recordBytes=3_072_000 是 gs32 的 LOCAL FORK，upstream gs64 是 2_764_800；layers=48 不含 MTP），建议 ExpertStore.init 加几何断言。
- 实测数据复核（脚本自算）：专家 77.07GB(71.78GiB)、其它 36.14GB、检查点合计 113.21GB(105.44GiB)、ngram_table.bin 32.0GB(29.8GiB)。文档 §2.2 的 105.4 是 GiB，其余是 GB——单位混用，已知。

## 记忆修正

- 上一轮记录的「P0 散射可能整池拷贝」已解除：就地写成立，前提（唯一引用）已写进文档踩坑 13。

## handoff 文档补全拍板（用户指令「你觉得怎么合理怎么正确怎么来」，09:22）

三处挂起开口全部核实并写进 `expert-streaming-handoff.md`：

1. **Step 3 拍板（关键新事实）**：`appendHybridMlpWeights`（transformer.zig:19350）对 `.moe`
   是 **MoeMlpWeights 整结构体遍历**，ctx 非空就 append 进加载末尾批量 eval（6779–6858，
   mlx_eval@6856）→ **switch_* 绑定即物化**，「moeMLP2 不引用就没事」不成立。拍板：
   ModelConfig 加 `expert_stream` 字段；shouldKeepWeightKey 跳过 `[language_model.]model.layers.{d}.mlp.switch_mlp.`（**放行 mtp 键**）；initMoeLayers 流式绑 null-ctx；
   moeMLP2 入口 `gw.ctx == null` 判别式决定走池（局部变量替换）或原路径；不用 switch_* 绑池块
   （retain 句柄破坏捐赠）。批量 eval 后（qwen4 分支 6860）构造池 → active_memory 量准。
2. **MTP 真相**：loadQwen4Mtp（11847）只看 `language_model.mtp.fc_hidden.weight` 存在
   （已验证存在于 of-00023 分片）→ MTP head 默认加载，不 config mtp_num_hidden_layers=0。
   草稿层 initMoeLayers(prefix="language_model.mtp") 绑**自己那套** 512 专家（1.57GB，惰性
   到第一次 qwen4MtpForward 才物化）——旧稿「草稿层共享主干专家」错误已改。
   transformer.zig:6275 "not wired into spec decode" 注释过时（generate.zig MtpHeadRef.qwen4
   已接线，scheduler.zig:3657 entry.mtp，mtp_enabled 默认 true）。
3. **indexShardSet 无通配问题**：model_discovery.zig:252 读 index.json 的 weight_map；
   Vontra index 列全 23 分片（含 of-00023），MTP 分片正常加载。`.clean` 后缀文件不匹配。

另：验收 #5 改记录型（<5 tok/s 视为 bug，最可疑散射退化）；新增踩坑 18/19；绑定分支锚点
修正 18914→18737–18757。initMoeLayers 锚点漂移说明：文档复核与实际有 ~160 行漂移，以
grep 为准。
