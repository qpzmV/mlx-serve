# mlx-serve — project context for AI

Native Zig server running MLX-format LMs on Apple Silicon; OpenAI/Anthropic/Ollama-compatible HTTP APIs + native media generation. No Python. This file is the COMPRESSED layer; detail lives one hop away.

## Detail map (read on demand)

- `docs/reference.md` — deep detail: per-file contracts, media-gen schemas, API surfaces, LAN design, observability, embedded engines, arch numerics (dsv4/inkling/H3, Kokoro, runtime LoRA), website/tier list, licensing. Read its section BEFORE deep work on a subsystem.
- `docs/gotchas/{tool-calling,server-http,engine-mlx,models-media,app}.md` — full war stories behind every rule in `## Rules`.
- `tests/CLAUDE.md` — integration-test matrix. `app/CLAUDE.md` — Swift app layout + rules (auto-load in their dirs).
- Skills: `/release` (pre-release checklist, CalVer, CHANGELOG), `/bench` (llmprobe methodology + comparison traps).
- `containers/{agent-shell-mlxserve,guest-kernel}/` — Agent Sandbox guest image + kernel, two pinned artifacts that BUMP TOGETHER. Detail: `docs/reference.md`.
- `website/` — GitHub Pages site + `llm-tier-list/`. Design: `docs/reference.md`. Guards: `tests/test_website_pages.sh`, `tests/website_tier_list_logic.mjs`.
- **Growth policy (ENFORCED)**: this file stays under 100k bytes and EVERY rule bullet is ≤ 3 lines. New gotcha = 1–3-line rule here + full story in `docs/gotchas/*.md`. New subsystem = one Layout row + a `docs/reference.md` section. App content → `app/CLAUDE.md`. Never paste war stories or measurements here.

## Stack

Zig 0.17 (pinned nightly via `scripts/fetch-zig.sh`; brew 0.16 no longer builds); mlx + mlx-c PINNED SUBMODULES (`lib/mlx-src` v0.32.2, `lib/mlxc-src` 56b2d39 = PR #127) self-built NAX-enabled by `scripts/build-mlx.sh` into `lib/mlx/` (FFI `src/mlx.zig`); jinja.cpp (wangzhaode, Apache-2.0, NOT llama.cpp's) as `lib/jinja_cpp/libjinja.a`; stb_image + libwebp; safetensors; BPE. Embedded engines: ds4 (`lib/ds4`, DSV4-Flash GGUF) + libllama (`lib/llama`, generic GGUF).

## Layout (`src/`)

| File | Role |
|---|---|
| `main.zig` | Entry, CLI flags + subcommands (`run/pull/list/serve/launch`) |
| `cli.zig` | Ollama-grade CLI: alias → HF repo, resumable pull into `~/.mlx-serve/models/<org>/<repo>`, `list`, `run` REPL |
| `launch.zig` | `mlx-serve launch <agent>` (claude/pi/omp/opencode/opencode2/codex/hermes/aider): reads `/v1/models` (models + ADVERTISED context), writes configs into `~/.mlx-serve/<agent>/`, starts the app if the server is down. Swift `CLILauncher`+`AgentConfigs` is the twin (omp `PI_CODING_AGENT_DIR`, codex Responses-only `CODEX_HOME`, hermes `HERMES_HOME`; opencode2 `XDG_CONFIG_HOME` + monitor plugin) |
| `mlx.zig` | mlx-c FFI |
| `model.zig` | Config parse + safetensors loading |
| `tokenizer.zig` | BPE; single special-token splitter (first-byte-bucketed); per-model `digit_group` |
| `transformer.zig` | Forward pass, arch dispatch (attention/MLP/MoE/GatedDeltaNet), quant resolution, custom kernels (`msv_attn_p256`, `verifyQmm` lanes incl. NAX) |
| `generate.zig` | Generation, sampling, PLD/drafter/MTP orchestration, `StallClock`, prefill chunking, loop-stop tiers |
| `reasoning_protocol.zig` | Bounded reasoning/header masks, recovery, and authoritative JSON response routing (see `docs/reasoning-protocols.md`) |
| `chat.zig` | Chat templates (ChatML/Gemma/Llama-3/Jinja2), thinking tags, tool-call parsing/repair/coercion |
| `vision.zig` / `qwen_vision.zig` + `mrope.zig` | Gemma SigLIP / Qwen3-VL ViT + M-RoPE |
| `muse_vision.zig` / `lfm2_vision.zig` | Muse-Glimmer ViT / LFM2-VL SigLIP2-NaFlex tower + projector + tiling |
| `server.zig` | All HTTP: `/v1/*` (chat/completions/messages/responses/embeddings/load/unload/models), media endpoints, `/metrics(.json)`, WS, Ollama glue, `--api-key`, console at `GET /` (`src/html/` as `{s}` args, renders with NO model). Embeddings: BERT + EmbeddingGemma, per-checkpoint pooling, `--embedding-max-length` |
| `lan.zig` | LAN sharing: Bonjour, `SharedSet` + `routeClass` allowlist, `<id>@<peer>` mirroring, streaming proxy. Pure transport |
| `model_settings.zig` | Per-model settings (`~/.mlx-serve/model-settings.json`, keyed by model path): `ctx_size`, `kv_quant`, `mtp`; read at every load construction site, stamped on `ModelConfig.{ctx,kv_quant,mtp}_override` |
| `providers.zig` | Upstream OpenAI-compatible chat providers (`~/.mlx-serve/providers.json`): background `/v1/models` probe, `<id>@<name>` rows (`models` = filter, or the list for a listless provider), curl-backed `/v1/chat/completions` proxy. `GET /v1/providers`, `POST /v1/providers/reload` |
| `metrics.zig` | Lock-free zero-when-off observability (`--metrics`): `vllm:`+`mlx_serve:` Prometheus + JSON |
| `ollama.zig` | `/api/*` translation, SSE→NDJSON `Sink`, tags/show/ps, `resolveName` |
| `gen.zig` | Unified media gen: modality-named engine slots, `detectModality`/`peekModelType`, per-request handlers, img2img/edit/LoRA, residency estimators |
| `krea.zig` / `flux.zig` | Image backends (Krea-2-Turbo / FLUX.2 klein 4B+9B); `MixedLinear` infers quant geometry |
| `multipart.zig` | RFC 7578 form parsing, zero-copy `Part` (only non-JSON shape: `POST /v1/images/edits`) |
| `mage_flow.zig` | MageFlow Turbo/Edit: flow DiT + DiCo VAE + Qwen3-VL TE; `MfLinear` shared with H3; DiT/TE bf16, VAE f32 (load-bearing) |
| `hunyuan3d.zig` / `hunyuan3d_paint*.zig` | 3D shape + texture paint; converted layouts BAKE OUT per-head QKV interleaves — never "fix" it |
| `acestep.zig` | ACE-Step music (Qwen3 encoder, AdaLN DiT, Euler flow-match, Oobleck VAE 48 kHz; Snake/encode f32) |
| `music3.zig` | MiniMax Music 3: Qwen3-8B global LLM (batch-2 CFG) + depth decoder → hidden states condition a flow DiT (temb as TOKEN) + Snake/DAC vocoder 44.1 kHz |
| `ltx_video.zig` / `ltx_audio.zig` | LTX video (one/two-stage/HQ, i2v, a2vid) + audio VAE/BigVGAN. `LtxVersion` (from `model_version`) keys 2.3-vs-2.5: text encoder, `ff_bias`, `keyframes_abs_pos_embedding` |
| `ltx_diffvae*.zig` | LTX-2.5 DiffVAE decoder (`"decoder":"diffusion"`): geometry/tiling, fused 3D NA Metal kernel, MLX pass. Sampler contract is MEASURED (x0, 1 step, timesteps x1000) |
| `minimax_h3*.zig` | MiniMax-H3 text-to-audio-video: joint video+audio DiT, staged residency, fast recipe, Turbo LoRA, chained windows — detail in `docs/reference.md` |
| `tts.zig` | Qwen3-TTS incl. ECAPA-TDNN voice clone |
| `kokoro.zig` / `kokoro_g2p.zig` | Kokoro-82M TTS + text→IPA G2P (no espeak — GPLv3) |
| `marching_cubes.zig` / `glb.zig` / `uvwrap.zig` / `rasterize.zig` / `texinpaint.zig` | Pure-Zig mesh/GLB/xatlas/rasterizer/inpaint (zero MLX, hermetic tests) |
| `preview.zig` / `latent_rgb.zig` / `jpeg.zig` | Opt-in per-step video previews (#208): published latent→RGB map per backend (GENERATED — `tests/dump_latent_rgb_factors.py`), temporal pick + filmstrip, bilinear resize, baseline JPEG. Zero MLX; `zig build preview-test` is the Linux-runnable graph |
| `responses.zig` | Responses API pure data: parser, envelope, `ResponseStore`, compaction |
| `ws.zig` | RFC 6455 framing (server-side) |
| `pld_index.zig` | PLD n-gram index (`findMatch`, `ngramRepeatScore`) |
| `prefix_cache.zig` / `kv_disk_cache.zig` / `kv_disk_writer.zig` | Hot prefix cache + SSD tier (`--prefix-cache-disk`, default OFF); SSD-first mode (qwen4_exp) + its background writer thread |
| `drafter.zig` | Gemma 4 assistant drafter (cross-attention spec-decode) |
| `dflash.zig` | DFlash block-drafter: config-contract detection (root OR nested `dflash_config`), per-request context cache, block forward; DFlash2 adds `selectPath` + 2-tap grouped convs; trunk seam = `ForwardCtx.capture_layers` + `rawEmbedding` |
| `mtp.zig` | Qwen 3.5/3.6/3.8 native MTP head (sidecar OR in-checkpoint `mtp.*` via `resolveMtpSource`; per-weight quant re-solve; committed-history cache) |
| `diffusion.zig` | DiffusionGemma block-diffusion canvas loop |
| `deepseek_v4.zig` | DeepSeek-V4-Flash NATIVE arch (module-owned decode state on `Dsv4Model.dec_state`, NOT the KVCache) |
| `qwen4_exp.zig` | Qwen3.8-Flash-Next (`qwen4_exp`) host side: n-gram hash (splitmix multipliers, per-head primes, eos-segment shifts) + the mmapped 4-bit `ngram_table.bin` row gather. Trunk forward = `transformer.forwardQwen4With` (hyper-connections, PLE, QSA mask over `gatedFullAttnWith`) |
| `scheduler.zig` | Slots, inference thread (sole MLX caller), queues, batching, loop-stop guard, spec wiring, single-flight admission |
| `round_cost.zig` | Measured per-model/width/KV-bucket spec round-cost table (`Transformer.round_cost`), persisted |
| `model_discovery.zig` / `model_registry.zig` | Discovery (two-level org/name, multi-root, GGUF classification, stub meta), multi-model registry |
| `arch/ds4.zig` / `arch/llama.zig` (+ `*_ffi.zig`, `lib/llama_shim`) | Embedded-engine bridges |
| `ane.zig` + `lib/ane/` | ANE prefill offload (`--ane-prefill`, opt-in, LOSSY int8/fp16, M4-and-below): SwiGLU-MLP + fused GDN in-proj MIL programs on the private AppleNeuralEngine framework (`msv_ane_*`, attribution in NOTICE), `/props` `"ane"` + `mlx_serve:ane_*`. Rules: `docs/reference.md` "ANE prefill rules" |
| `lora.zig` | Runtime unfused STACKED LoRA (8 max, summed never merged) across QLinear/MixedLinear/MfLinear |
| `status.zig` / `log.zig` | TUI status bar; leveled logging + file sink (`~/.mlx-serve/logs/mlx-serve-<port>.log`, 32 MB rotation) |
| `format_corpus_test.zig` / `tool_traffic_replay_test.zig` | Hermetic format corpus + real-traffic replay (`src/fixtures/tool_traffic.jsonl`) |

CLI flags: `--model --serve --host --port --prompt --max-tokens --temp --top-p --top-k --ctx-size --config-overrides --embedding-max-length --timeout --reasoning-budget --no-vision --pld --pld-draft-len --pld-key-len --drafter --draft-block-size --no-mtp --mtp --mtp-depth --mtp-history-window --max-mtp-ctx --ane-prefill --ane-image --ane-video --ane-audio --ane-split --dspark --decode-attn-quant --no-decode-attn-quant --kv-quant --kv-attn-mode --prefix-cache-entries --prefix-cache-mem --prefix-cache-disk --max-concurrent --skip-mem-preflight --wired-margin-gib --mtp-head-kv-quant --metrics --api-key --lan-share --lan-discover --lan-name --no-drafter --no-tool-autocorrect --no-prevent-sleep --ssd-streaming --no-ds4-mtp --model-dir --log-level --log-file --version --help`

Sampling defaults for omitted fields: body > launch flags > model `generation_config.json` > hardcoded (1.0/1.0/off). Missing generation_config = wild-sampling signature.

## Building

- First-time: `./scripts/fetch-zig.sh` stages the pinned Zig at `.zig-toolchain/`.
- Zig caches configure-time subprocess output: after a toolchain/SDK change `rm -rf .zig-cache` or the link references a ghost path.
- **ALWAYS `zig build -Doptimize=ReleaseFast`, never bare `zig build`** (Debug 2–4× slower ⇒ fake regressions). `zig build test` does NOT refresh `zig-out/bin/mlx-serve` — rebuild before any live A/B.
- Swift app: `bash app/build.sh`. The two bundle binaries move together.
- mlx + mlx-c: submodules built by `scripts/build-mlx.sh` (deployment target 26.2 → NAX kernels; script + `tests/test_mlx_staged_nax.sh` ASSERT `*_nax` in the metallib). Min macOS 26.2. Bump = checkout tag → rerun → re-diff `src/mlx.zig` externs. Brew: webp ≥ 1.6.0.
- Rebuild Jinja after `lib/jinja_cpp/*.cpp` changes: compile the 7 `.cpp` (`clang++ -std=c++17 -O2 -DNDEBUG -I .`) into `obj/` and `ar rcs libjinja.a obj/*.o`.

## Testing — TDD is mandatory

Order: (1) failing test FIRST, for the right reason; (2) minimum code to green; (3) full suite (`zig build test` 6/6 0 fail + `bash app/test.sh`/`swift build` + relevant `tests/*.sh`); (4) refactor. A live curl is a sanity check, NOT a test.

Feature = unit test that fails without it (+ integration script if HTTP-observable). Bug fix = regression test red→fix→green, red-on-revert. Cross-arch = cover every touched arch. Refactor = characterization test first. UI/build scripts = factor a pure helper and test that.

**Class bugs get class guards.** A live failure revealing a CLASS ships: the instance regression test; a corpus entry or universal invariant in `src/format_corpus_test.zig`; a rule here + story in `docs/gotchas/`.

Hermetic suites: `zig build test -Dtest-filter="format corpus"`, `-Dtest-filter="tool traffic"`. Full matrix: `tests/CLAUDE.md`.

## Releases & benchmarking

- Process/CalVer/CHANGELOG: `/release`. Per-release perf gate = `./tests/bench.sh` on the FINAL tree vs the previous column in `benchmarks.md` (ONE new column per release).
- Methodology: `/bench` (llmprobe measures; bench.sh drives mlx-serve, `--url` probes another engine). Only diff same-methodology cells (llmprobe from 26.8); spec cells are variance — sample across runs/boot-orders, same-session ratios only, name the engine in every win, and an A/B arm is proven by ENGAGEMENT lines in its log. Record = `benchmarks.md` + `~/claude-tmp/bench-<tag>/`.

## Conventions

- Minimal DRY Zig; tests at the bottom of each source file; shell integration tests in `tests/`. Env levers only for paths with two arms worth comparing (lossy/tradeoff), never for an obvious win/fix.
- Inference thread is the SOLE mlx caller (even frees) — media gen posts to `gen_queue`, never a gpu mutex. A long gen blocks chat decode (accepted).
- Concurrent requests batch-decode on pure-attention archs + the qwen3_5 family incl. `qwen3_5_moe` and `qwen4_exp` (`configBatchesDecode`); `--max-concurrent` sizes the submit queue, not a decode gate. The per-slot verdict is a `BatchVerdict` reason: `[batched] slot serial: <reason>` once per slot, `/props` `batching`, `/v1/models` `batched_decode`, `mlx_serve:decode_serial_total{reason}`. Slots entering a batch mid-generation drain lazy pipeline state first.
- A batched decode group is capped by PADDING WASTE, not slot count (`batchedKvKeepCount`, `MAX_PAD_WASTE` 1.5 — must stay < 2.0): `padAndStackBatchedKV` pads to the group's longest, an unbilled per-tick transient. Group sorts by kv_len, keeps the largest prefix under the bar; longest slots fall to serial.
- A batched-decode guard that only runs at N=1 pins a shape that never ships: `MLX_SERVE_FORCE_BATCHED=1` at one slot still has `batch == 1` inside the forward (decode-attn-quant + fused QK-norm gates key on it). `tests/test_batched_equivalence.sh` runs a real two-stream arm; both kernels log `[batched] ... engaged (slots=N)`.
- A cold prefill YIELDS to decode ticks at chunk boundaries (`scheduler.interleaveDecodeTick`); greedy byte-identical, idle cost zero, `MLX_SERVE_PREFILL_INTERLEAVE=0` restores. Guard: `tests/test_prefill_interleave.sh`.
- KV reuse via prompt-prefix matching; invalidated after tool calls + pad-only gens; hot cache spills to SSD; RAM invalidation propagates to disk.
- Chat templates live in model dirs; Jinja renders with fallback formatting.
- **Diffs are read by a human. Keep them small.** A comment says what the code cannot (a non-obvious WHY, a contract, a unit), in one to three lines. Never: bug history, measurements, audit trails, review item numbers, dates, "PR #NNN", what an older commit did, or a restatement of the code. That belongs in the commit message and, if it is a rule, in `docs/gotchas/*.md`.
- **No source-scan tests** (`@embedFile` + "this string appears in that function"): they pin text, not behaviour, and pass against their own needles. Test the behaviour or state the rule in a comment. A test comment is one line saying what the bar is.
- **One story per gotcha, one line per rule.** A gotcha entry is the defect, the cause, the fix and the guard in under 20 lines, written once after the work lands. No round-by-round logs, no ledgers, no "what the reviewer said". CHANGELOG: one user-facing sentence per change, no provisional numbers.

## Supported architectures

Dispatch on `config.json` `model_type`. GGUF bypasses MLX → embedded engine by header (`gguf_meta.preferredEngine`: antirez DSV4-Flash → ds4, else llama.cpp).

| model_type | Notes |
|---|---|
| `gemma4`, `gemma4_text` | `language_model.model` prefix; SigLIP vision; clipped linears, PLE |
| `diffusion_gemma` | Gemma 4 26B-A4B trunk, BLOCK-DIFFUSION (diffusion.zig): ≤48-step canvas denoise; PLD/drafter/MTP/batching/prefix-cache never apply; instruct-only |
| `gemma3`, `gemma3_text` | + flat text-only sibling; EmbeddingGemma encoder when `use_bidirectional_attention` |
| `qwen3` | QK norm |
| `qwen3_5`, `qwen3_5_moe(_text)` | GatedDeltaNet + optional MoE, shared expert; Qwen3-VL vision. Qwen3.8 packs serve on this arch |
| `qwen3_next` | DeltaNet |
| `nemotron_h` | Hybrid transformer + Mamba2 (`backbone` prefix) |
| `lfm2`, `lfm2_moe`, `lfm2_vl` | Hybrid gated conv + attention; `lfm2_moe` = sparse MoE past `num_dense_layers` (sigmoid routing, selection-only `expert_bias`, no shared expert); dense MLP `w1/w3/w2` OR `gate/up/down_proj` (probed); `lfm2_vl` = siglip2 tower + projector, NaFlex 64-256 merged tokens, tiling past `max_image_tokens x 2.0` into 512px tiles + thumbnail (`<|img_row_R_col_C|>`/`<|img_thumbnail|>`) |
| `hy_v3` | Hunyuan 3 MoE (expert container probed) |
| `laguna` | poolside Laguna S 2.1 (117.6B-A8.5B): nvfp4 experts, softplus attn out gate, YaRN + sliding, sigmoid routing, UNGATED shared expert. Serial |
| `inkling_mm_model` | Thinking Machines Inkling Small (276B-A12B, REAP-prunable). NO RoPE (RelativeLogits bias + causal short-convs); role-less markers; serial, spec off |
| `muse_glimmer` | Muse-Glimmer-30B (dense VL, GQA 32/2 hd128): sandwich norms (post-norm eps 1e-8), weight-less qk-norm folded into `attnScale()`, sigmoid attn out-gate, NoPE every 4th layer, RMS-normed embeddings, logits softcap 20. Harmony channels + ATEM tools; thinking silently ON with tools / OFF without (`defaultEnableThinking`), thinking-off commits ` to=user<|message|>`. Llama-3 pretokenizer. Vision via `muse_vision.zig` (window attn 3/4 layers, 2D RoPE, CHANNEL-major pixel-shuffle). DFlash via `-assistant` sidecar. Mirror `ddalcu/Muse-Glimmer-30B-MLX-Serve-8bit` |
| `llama`, `mistral` | Standard |
| `spark2_5` | XHToken Spark-X2.5 (1.7B/4B dense, hd 256, 16/4 heads): 3 sliding(512)+1 full layers with PER-TYPE RoPE (sliding full-rotary 1e4, full 25%-rotary 5e6), exact-erf GELU, plain norms, PER-HEAD sigmoid attn out-gate (`g_proj`, `attn_gate_headwise`), fused `q_k_v_proj` split at load (`attn_fused_qkv`), `model.embedding` tied head. DeepSeek-style template (`<think>` open/close by `enable_thinking`, GLM `<arg_key>` tools); tokenizer = DSV4 regex + `Digits` rule ⇒ per-digit. Packs `abenzerps/Spark-X2.5-4B-MLX-{4,8}bit` |
| `deepseek_v4` | DeepSeek-V4-Flash NATIVE (284B-A13B, 1M ctx; safetensors only). Serial, spec hard-off, prefix cache OFF, single-flight; DSML tools on OUR byte-pinned template; checkpoint 0731+ only; only our converter's layout; mirror `ddalcu/…mixed-2-3-8bit`. DSpark spec |
| `qwen4_exp` | Qwen3.8-Flash-Next (125B-A6B + 51B n-gram + 4B MTP): qwen3_5 GDN+MoE trunk inside 4 hyper-connection streams (`hcRead`/`hcWrite`, norms folded by the converter), n-gram PLE at layer 1 (host gather from `ngram_table.bin`, never resident), QSA sparse attention past 2048 tokens (`qsaMask`), `hyper_connection_mixer` replaces `model.norm`. Module state is READ-ONLY: text slots BATCH-decode (`forwardMoeBatchedDecode`), only the MTP slot is exclusive (`scheduler.slotExclusiveDecode`), prefix cache ON. MTP = the checkpoint's own QSA+MoE layer over the PRE-mixer stream (`Qwen4Mtp`, `MtpHeadRef.qwen4`, opt-in `--mtp`). Vision = the Qwen3-VL tower (`qwen_vision.zig`, `model.visual.` prefix) + M-RoPE through attention AND the indexer, decoding serially. Oracle `tests/dump_qwen4_exp_fixtures.py`, converter `tests/convert_qwen38_flash_next.py`; the 64 GB pack `ddalcu/…-iQ-MLX-3.3bpw` (52 GB resident, exact-bf16 imatrix via `tests/qwen38_flash_next_imatrix_collect.py`, per-layer widths via `qwen38_flash_next_iq_allocate.py`, bar = `qwen38_flash_next_score.py`) |
| `bailing_hybrid` | Ling 3.0 (BailingMoeV3): KDA + MLA hybrid MoE, `layer_group_size` → `full_attention_interval`; KDA = GDN with PER-CHANNEL gate (`_vec` kernel), BOUNDED-SIGMOID gate (`kda_lower_bound` REPLACES softplus), sigmoid out-gate; MLA = naive DeepSeek-V3 (ASYMMETRIC K192/V128 cache; `--kv-quant 4|8` ok, turbo refused); `noaux_tc` routing. Thinking ON; GLM tool tags. Mirror `rapid-mlx/Ling-3.0-tiny-MLX-4bit` |
| `*.gguf` | ds4/llama.cpp; GGUF presence WINS over stray config.json. ds4 DSpark: `--dspark` arms when a `-DSpark-` GGUF sits beside the model (gate keys on `mtpDraftTokens()>1` NOT `hasMtp()`); ~0 net on 0731 |
| `minimax_h3` | MiniMax-H3 text-to-audio-video: joint denoise, 17k+5 frame ladder, 24 fps, two partitions (fl2va/ref2va — `tasks` is the ONLY discriminator), Turbo LoRA, chained windows, fast recipe default-on |
| media types | `flux2*`/`krea*`/`mage_flow*`/`qwen3_tts`/`acestep`/`minimax_music3`/`AudioVideo` (LTX 2.3 + 2.5 by `model_version`)/`hunyuan3d*` → gen.zig slots (`mage_flow` has NO root config.json — classified from `model_index.json` by `gen.peekModelType` + `model_discovery.peekMageFlowIndex`, kept in sync) |

Models with `vision_config` but no vision weights disable vision. Embedded-engine detail: `docs/reference.md`.

## Unified media generation

One server, one registry — image/audio/video/3D coexist with chat. Engine slots are MODALITY-named unions on `LoadedModel`; new backend = one union arm + impl file. Gen runs on the inference thread via `gen_queue`; app flow load→generate→unload; headless starts idle. `model_discovery.isMediaModelType` and `gen.modalityFromType` are documented duplication — keep in sync. Downloads → `~/.mlx-serve/models` (Swift + Zig).

- `POST /v1/images/edits` = OpenAI multipart translated by `gen.openaiEditFormToJson` into the `mode:"edit"` JSON body; unhonored fields = NAMED 400.
- LoRAs are STACKED, ONE grammar across image/LTX/H3: `lora_paths`+`lora_scales` (cap 8, `gen.parseLoraFields`), summed at forward — never merged. Resident backends reconcile via `setLoras`; H3 pre-validates (`lora.validatePath`), Turbo = file 0.
- **FLUX real CFG** (`guidance_scale`+`negative_prompt`, `gen.ImageEngine.supportsGuidance`): distilled klein bakes guidance into the weights and takes NEITHER field (1.0 default = the unconditional forward never runs, `flux.ditVelocity` called once); the undistilled "base" 9B checkpoint (`flux2-klein-9b-base`, app preset `flux2Klein9BBase_Q4`) needs it — TWO forwards per denoise step, blended `uncond + scale·(cond−uncond)`. Same DiT geometry as the distilled 9B (checkpoint-derived), so nothing else about loading changes.
- Endpoint/field/backend detail: `docs/reference.md`. Guards: `tests/test_unified_gen.sh` + per-modality scripts; parity via env-gated cos oracles (`tests/dump_*_fixtures.py`).
- **A denoise step is the ANE case the LM prefill seam never was** (opt-in, LOSSY: `--ane-image` Krea, `--ane-video` H3, `--ane-audio` ACE-Step; ONE value `ane.media_offload` set in `main()`): batch job at the compute roofline; ONE compiled 256-row tile, looped (`ane.mediaMlp`), serves every step AND every request size, so the ANE cache never grows per size. M4 Max: Krea 1.30x, H3 1.22x/step, ACE-Step 1.33x; M4 base ACE-Step 1.58x. Tables: `docs/reference.md`.
- **The media share is CALIBRATED once per (chip, model), then STICKS** (`ane.planMediaOffload` → `calibrate`): block 0 alone, compiled at the probe's seed, timed on the ANE and GPU over the same 4096 rows, the GPU fed the dtype the MLP really sees (bf16 ones read ACE's f32 GPU 27% fast); later builds reuse the set's `calibrated share=` tag (`ane.cachedShare`; an explicit `--ane-split` never is), since re-solving recompiled + pruned on size and probe jitter.
- **The ANE compile cache is capped by FREE DISK and pruned per LINEAGE** (`msv_ane_cache_lineage`: same seam+shape, other share → gone on the next cold compile; byte cap = min(40 GB, volume room − 8 GB reserve)): a fixed cap above free space is not a cap, and two small-disk boxes shipped `ready: N/M`. The gate bills the build's f32 transient against a 4 GB swap floor (`mediaGateRefusal`), refusing by NAME.
- The seam is the channel-mode one (`ane.packUnitPlanes`/`readPlane`/`dequantToHostF32` shared with `transformer.zig`): ANE holds gate/up channels [0..k) + the matching down K-slabs, GPU the complement, partials ADD. H3's fc1 is FUSED, so its complement is TWO row views (`aneBuildRest`).
- **The ANE graph is fp16 END TO END, so a partial-sum seam must SCALE** (`ane.OUT_PLANE_SCALE` 256, folded into the `up` copy at build time, multiplied back on read — exact: per-row int8 puts it in the row scale, and `up` is linear into `silu(gate)*up`). Unscaled, H3 saturated to INF from block 36 and rendered BLACK; Krea only lost precision (cos vs GPU 0.996 -> 0.9993).
- **A LoRA-attached block DECLINES** (`aneBlockEligible` in both): the adapter is summed at forward from the FULL activation, half of which never leaves the ANE program. Turbo binds `blocks.N.mlp.fc1/fc2`, so H3's fast path is GPU-only until the LoRA is folded into the int8 snapshot.
- **GPU work built AFTER a blocking ANE wait is serial, and a small piece of it is LAUNCH LATENCY, not rows** (`ane.mediaMlp`): a 23-row tail after the tile loop cost ACE 30 s 7% of diffusion. Every GPU piece goes out with the complement BEFORE the loop; a partial tile pads onto the ANE only past `tail > T x (1 - share)` (`ane.mediaTilePlan`).
- **H3 stages the DiT per REQUEST, so the ANE build is paid per request** (cold 32 s, warm 8 s + a 4.9 GB int8 copy): noise against a 275 s/step dense render, dominant on a short one. Caching the engine across requests is owed.

## HTTP APIs

- **OpenAI chat/completions + Responses**: usage ALWAYS carries `prompt_tokens_details.cached_tokens` (ONE `formatChatUsage`); thinking opt-ins = `reasoning_effort` OR `enable_thinking` (`reasoning_budget_tokens` outranks); `n>1` 400s. `/v1/responses`: every SSE event has `sequence_number`, `background:true` → 400, stateful via `ResponseStore`, WS via Upgrade, NO `[DONE]` on WS.
- **Continuing a partial reply** (`chat.continuationRequested`/`continuationPrefill`): trailing assistant message becomes a PREFILL (whitespace trimmed). `/v1/chat/completions` takes `continue_final_message` EXPLICITLY; `/v1/messages` INFERS; `/v1/responses` neither. Tool-call replies never continue; in the tokenize-cache key; ds4 can't (`continuationRejectReason`).
- Context-overflow 400s name BOTH counts (`contextOverflowMessage`); the app renders it as a card.
- **Anthropic `/v1/messages`** (Claude Code): typed blocks, `input_schema`→`parameters`, stop-reason map incl. `stop_sequence` echo, full SSE block lifecycle. Launcher env: `ANTHROPIC_BASE_URL` + dummy keys + `ANTHROPIC_DEFAULT_*_MODEL=mlx-serve`.
- **Ollama `/api/*`**: pure translation, no-model endpoints answered pre-scheduler; `resolveName` handles `name:tag`. One path must never register under TWO ids (`registry.peekByPath` — double-residency OOM).
- `/v1/models` rows carry `context_length`+`max_model_len` at TOP level twinning `meta.context_length` (#188): discovery clients never read `meta.*`. Both emitters. Guard: `tests/test_models_capabilities.sh` [4b].
- **Providers**: `<model>@<name>` from `~/.mlx-serve/providers.json` proxies `/v1/chat/completions` ONLY (other surfaces = named 400) through curl with the provider's key; rows ride `/v1/models` with a `provider` badge while the probe answers, a non-empty `models` list FILTERS the provider's list (and IS the list when it has none), none when unreachable. App: "Pick…" fetches the list into a checklist. Never shared to the LAN. Design: `docs/reference.md`.
- **LAN sharing**: proxy is a TRANSPORT; keyless gate = `routeClass` × `SharedSet`; `<id>@<peer>` mirroring; loops impossible by construction (self-token + tunnel marker, one hop). Design: `docs/reference.md`.
- **Observability** (`--metrics`): zero cost off; TTFT at prefill completion; live tok/s via ONE atomic per tick. `--api-key`: loopback exempt; `/health`+OPTIONS open; `constTimeEql`. No admin surface.

## Tool calling (server pipeline)

With `tools`, tokens buffer for detection (all tag families + raw JSON); thinking buffers separately. Parse chain: strict → tolerant repairs → truncation salvage, then the ONE chokepoint `server.parseToolCallsForRequest` = parse → inferred-name filter → parallel clamp → buried-param hoist → schema coercion (the last two gated by `--no-tool-autocorrect`; emitted `arguments` ALWAYS valid JSON). Serialization `chat.serializeMessagesJson`: role "tool" native, args as JSON STRINGS, every string via `appendJsonString`. Streaming: full args in ONE SSE delta, thinking → `reasoning_content`. Fallback `fallbackFormatChat`. Client: `app/CLAUDE.md`.

## Prompt-based skills / downloads / debugging

- Product skills: `~/.mlx-serve/skills/*.md` (frontmatter trigger substring → body into system prompt; `SkillManager` rescans on mtime).
- `DownloadManager`: streams to `.partial`, Range resume, 3 retries, cancel preserves partial, size-matching files skipped.
- Server log `~/.mlx-serve/logs/mlx-serve-<port>.log` is THE post-mortem file (`--log-level debug`). Grep: `jinja error:`, `[cache]`, `<- N+M tokens`, `tool_msgs=`, `[spec-stats]`, `spec-gate:`, `[loop-stop]`, `[lan] proxy`, `[disk-cache]`, `[hot-cache]`, `[admission]`, `[providers]`. Capture: `MLX_SERVE_RAW_DUMP_FILE=<abs>` → `tests/harvest_tool_traffic.py`. Reproduce tool bugs `stream:false` first; `pkill -f mlx-serve` between KV-poison tests.

## Rules (distilled gotchas — stories in docs/gotchas/; every bullet ≤ 3 lines)

### Tool calling & formats (→ docs/gotchas/tool-calling.md)

- **Control bytes**: ONE raw byte <0x20 in history kills the strict render → SILENT `fallbackFormatChat` (model loses its stop token). Everything through `appendJsonString`; wrong-family tags out ⇒ suspect silent fallback first.
- **A NUL byte in any message TRUNCATED the rendered prompt** (`grep -a` output pasted into an agent turn): the `\u0000` escape decodes back to a real NUL inside jinja and the shim's bare `char*` was read with `std.mem.span`, so the prompt ended mid-user-turn with no assistant header and the model answered with an immediate EOS (`P+0 tokens … [stop]`, empty agent turn). `jinja_render_chat` returns its LENGTH; byte-level BPE round-trips 0x00. Tell: the same `prompt=` count on consecutive turns.
- **`developer` is OpenAI's spelling of the system turn and every template we serve reads it as `system` or raises** (`chat.canonicalRole` at both parse sites — chat + Responses; issue #348: pi sends it for reasoning models unless `supportsDeveloperRole:false`, Qwen's template raised, the SILENT generic fallback lost the stop token and pi "stalled").
- **A `chat_template` value can be a POINTER** (`{% include 'chat_template.jinja' %}`, transformers ≥5, issue #169): `chat.isIncludeStub` reads an include-only value as "no inline template" so the sidecar loads. Grep the log for `jinja` first.
- **A JSON-dialect call cut INSIDE the object still names its tool** (`chat.truncatedJsonCallName`, depth-1 `"name"` only): the Hermes JSON arm had no NAME + `{}` salvage, so a 4 KB `edit` that hit EOS mid-string vanished as an empty `stop`. Shipping the raw markup as content is NOT the fix.
- **Model-mangled arg JSON**: strict parse fails → `looseRepairToolCallJson`; never drop the whole call. **Truncated opener**: recover NAME + `{}` — NEVER ship partial values.
- **Delimiter-drop tolerance** (hy3): a tag parser never bails on ONE missing delimiter; plural-wrapper recovery keys strictly on the SUFFIXED `<tool_calls:` form.
- **A `<tool_call>` body carrying `<function=` is the XML dialect and is read FIRST** (qwen 3.5+ template mandates it): the JSON branch once snapped a package.json out of a `content` parameter and shipped its `"name"` as the tool. A parameter VALUE never decides the call.
- **A `<parameter>` VALUE may spell the dialect's own close tags** (`hermesValueEnd` = LAST `</parameter>` before the next opener; `hermesParamSpanEnclosing` skips values when scanning for `</function>`/`</tool_call>`): first-occurrence scans emptied or dropped a `content` documenting the format. The TRUNCATED-call branch reads `<function=` first too; `isJsonNumber` (JSON grammar, not `parseFloat`) decides literal vs string.
- **A hand-written partial-prefix ladder DERIVES its test from the marker** (MiniCPM5 `<function`): an enumerating test copies the array's gaps — `<funct` was missing from both. A gate check is a strict SUPERSET of its parser, else a real opener flushes AND duplicates.
- **A `</think>` inside a tool ARGUMENT is payload** (`chat.thinkCloseIsToolCallPayload`): decline a close whose nearest preceding tool opener is still OPEN AND whose block closes afterwards — both halves load-bearing.
- **Near-repeat loop tier** (`generate.isNearRepeatTailLoop`): 1024-token window, THREE ratios must ALL be low (distinct ≤0.12, 4-grams ≤0.35, novelty ≤0.10); the third (PROGRESS) tells a loop from procedural code. Bar errs toward acquittal.
- **Loop-stop cuts are truncations**: `finish_reason "length"` (`scheduler.loopStopReason`), `[loop-stop]` logged, no fragment values; `finish_details:{"type":"repetition_loop"}` on chat+completions; non-streaming trimmed to the span start (`loopTrimmedIds`, `MLX_SERVE_LOOP_TRIM=0`). Guard: `tests/test_loop_stop_signal.sh`.
- **A container param string with a key repeated at the SAME value still coerces** (#402, `parseContainerAllowingRepeats`: first-wins and last-wins parses must agree, a CONFLICTING repeat stays a string): Qwen Code's `questions` array spelled `header` twice per item and both strict parses rejected it.
- **Types come from the SCHEMA, never the value's spelling** (`coerceToolArgsToSchema`; undecidable → untouched). ONE chokepoint `server.parseToolCallsForRequest` — never call `chat.parseToolCalls` from a handler.
- **Buried required params**: `hoistMisplacedRequiredParams` lifts only on all-schema-read unanimity. Pristine args = the parse layer is innocent.
- **Heuristic raw-JSON inference must name a DECLARED tool** (`filterInferredBySchema`); explicit tag calls never filtered; new heuristics set `.inferred`; not autocorrect-gated.
- **Hard invariants (replay-pinned)**: emitted args ALWAYS valid JSON; every converter escapes + dedups; coercion never worsens conformance; broken output stays honest. Harness: `src/tool_traffic_replay_test.zig`.
- **A Hermes param value keeps its own whitespace** (`chat.stripHermesValueFraming`): the template frames it with EXACTLY one newline per side and that is all the parser strips — trimming ate an `old_string`'s indentation. Padded SCALARS still type from their spelling (`isJsonLiteral` probe).
- **Gemma dropped `<|"|>`**: rich bare values run to the CONFIRMED closing delimiter or top-level final `}`, never the first `,`/`}` inside markup.
- **Think-tag leaks**: strip pos-0 unclosed openers; `trimTrailingThinkClosers`; universal no-tag-leak corpus invariant covers new entries.
- **Unparsed tool markup never rides out as reasoning OR content** (`chat.trimLeakedToolMarkup`): one cut at the first wrapper opener, applied ONCE; `/v1/messages` cuts at emission; streaming end-flush concatenates BEFORE cutting.
- **Streaming + tools + thinking**: buffer until pattern resolution; reasoning streams INCREMENTALLY on the tools path (`.hold_thinking` + `chat.unstreamedReasoning`, never a resend; a BUDGET keeps buffering). Pinned by corpus streaming replay + `tests/test_messages_stream_thinking_tools.sh`.
- **The streaming think gate scans with a CURSOR** (`chat.ThinkScan`, O(n)); close tag cannot latch once a `tool_call` substring is seen — falls back to the full scan.
- **Pythonic tool calls (lfm2/LFM2.5)**: call-expression grammar; TYPES live in the SPELLING; marker-gated ⇒ additive; truncation ships NAME + `{}`.
- **A template can open `<think>` unconditionally** (LFM2.5): whether a prompt ends inside a think block is a property of the RENDERED BYTES (`server.promptOpensThink`), never ANDed with `enable_thinking`; thinking-off + prompt-opened ⇒ a stream arm DROPS the block.
- **A model can open its OWN think block when the template did not** (Gemma 4 `<|channel>`+`thought`, a bare `<think>` token on LFM2.5-8B-A1B, muse's ` to=self<|message|>` header at position 0): the plain arm latches via `chat.modelThinkOpener` (checked BEFORE the marker skip, which swallows `<think>`) and `promptOpensMuseHeader` arms the header skip at stream start. Bar = the stream/non-stream split invariant on a template that opens nothing; `tests/test_smoke_matrix.sh` runs it per arch.
- **`in_think_block` seeds from `prompt_opened_think` ALONE at every stream site, never the request flag** (LFM2-VL shipped its whole answer as reasoning with empty content). Class guard = source scan; bar = stream-vs-non-stream byte invariant (`tests/test_lfm2_vision.sh` [6/7]).
- **An integration assertion that a MODEL must think/answer/call is a checkpoint expectation**: assert the INVARIANT, branch on the model's choice.
- **A contract COMMENT is read as a spec** — pin it with a test or it gets filed as a bug (#94).
- **Assistant-history reasoning round-trips** (`Message.reasoning_content`, OMITTED when absent): reasoning-persisting templates (laguna, inkling) otherwise render nothink signatures from turn 2.
- **A template can raise_exception on OUR extra-context values** (Inkling, Qwen3.8): `serializeExtraContext` sniffs the family; tool-call `arguments` stay OBJECTS; history tool_calls carry `"id"`. Qwen3.8 = THIRD effort vocabulary (`xhigh|medium|low`); the thinking-off refusal is sniffed on the refusal string and only a refusing template gets `noThinkTailSuffix`.
- **A transcribed chat template is worth what it is PINNED against; whitespace is a token-level contract** (`tests/dsv4_template_ab.py`, byte equality over the shapes the server emits).
- **Inkling channel markers are single special tokens, but tool turns need more**: `streamShouldBufferForTools` buffers on the invoke marker and HOLDS bare-identifier segments after boundary markers.
- **A muse channel HEADER is ordinary text between single-token markers**: every stream surface HOLDS an unresolved header (incl. sub-`to=` prefixes) and drops header tokens (`museHeaderSkipNext`); `to=self`→reasoning, `to=user`/bare→content, else→ATEM tool (`splitMuseChannels`/`museStreamVerdict`). ATEM strings RAW, JSON keeps type.
- **An error echo teaches the model the error** (Inkling name salvage): NAME = trailing identifier run; body = BALANCED JSON; a parsed NAME never contains `<|` (corpus invariant); bare-JSON inference never runs on invoke-marker text; `applyFamilySamplingDefaults` fills top_p 0.95 when no generation_config.
- **Thinking-off is enforced in the PROMPT; generated reasoning is ALWAYS delivered** (`chat.noThinkTailSuffix`: muse commits ` to=user<|message|>` without tools, LFM2.5 gets `</think>` appended). Every delivery site splits via `splitThinkBlock(text, true, …)` — no strip-and-discard path.
- **Muse renders round-tripped reasoning as `to=self` HISTORY, so PRIOR-turn reasoning is dropped from the prompt** (`chat.dropPriorTurnReasoning`): only assistant messages AFTER the last user message keep `reasoning_content`. laguna/inkling untouched — per-family.

### Server, HTTP, lifecycle (→ docs/gotchas/server-http.md)

- **A constrained JSON payload offset is AUTHORITATIVE** (`reasoning_protocol.Delivery`, all three surfaces, stream + non-stream): the generator publishes where the schema payload begins and nothing after it is re-parsed, so a marker string inside JSON data stays data; markup cleanup never runs on it, and the routing buffers outlive serialization (Anthropic's thinking block). Guard: `tests/test_json_schema_protocol_routing.py`.
- **`messages.deinit(allocator)` frees the Message array and NOTHING it points at**: request media is owned by ONE `server.RequestMedia` beside the `messages` list; `Message` BORROWS the slice; a media list is only obtainable from `openImages`/`openVideos`/`openAudio`; slots are INDICES. Guards: source scan + a `std.testing.allocator` test on `RequestMedia`.
- **Select active-turn media from WIRE METADATA before decoding it**: a stateless client resends every historical data URL every turn, only the active media message is encoded. `activeWireMediaIndex` mirrors the parsed-message assistant/tool boundary on both chat surfaces. Guard: `tests/test_vision_prefix_cache.sh`.
- **A `seed` binds EVERY sampler with a fresh key PER DRAW** (`generate.seedKey` + `SamplingParams.draw`; `Generator.sampleLazy` is the one lazy sampler): the lazy path used MLX's global RNG (seed worked only with `logprobs`, replaying ONE key every step).
- **Logprobs are the MODEL's distribution, ids travel WITH values, entry belongs to the RETURNED token**: pre-temperature; argpartition+gather (`mlx_topk` is NOT descending); one-token delay (`Generator.pending_logprob`). Bar: temp-0 rank 1 == chosen token.
- **Streaming logprobs on BOTH surfaces**: `logprobs` is a SIBLING of `delta`/`text` (`ChunkExtras`, null never absent); ONE collector `StreamLogprobs`, drained against a HIGH-WATER MARK, shipped EXACTLY once. Guard: `tests/test_logprobs.sh`. Gaps: `logit_bias` unparsed, `/v1/responses` no logprobs.
- **`logprobs.content` describes `message.content`** — slice by `contentTokenRange`; streaming is structural (reasoning emitters never drain; content chunks `skipToContent`; empty-content arm = `dropPending` at all four sites).
- **NO string built from model bytes is guaranteed UTF-8** (a token is a BPE fragment; an invalid body is LOST, not degraded): sanitizing lives INSIDE the escaper — `chat.utf8Next` under `server.jsonEscape`, `responses.jsonEscape`, `chat.appendJsonString`, `ollama.writeJsonString`. Logprobs `bytes` keeps the exact bytes.
- **`/v1/completions` logprobs is an INTEGER and a different shape** (four parallel arrays); measure over the CHAT surface.
- **Liveness is a property of the SOCKET**: `beatStreamKeepalive` at the bottom of every streaming loop; emit on 5 s byte-silence. `StreamHeartbeat` (client) mirrors `StallClock` (server). GAP: Ollama sink drops SSE comments.
- **A missing tensor is a load ERROR, never `unreachable`** (#217): every non-optional weight getter (`getLayerWeight`/`getWeightFmt`/`getBert*`/`getLayerScaleOrEmpty`/`getLayerBias`) returns `error.MissingWeight` → named 503 via `loadErrorFromName`; one unloadable pack on disk used to end the process and every queued request.
- **`modelDiskBytes` bills the shards the INDEX names** (#274): `model.safetensors.index.json` `weight_map` is the set; a stray `.safetensors` beside it is dead weight and no eviction trigger. No index = every shard (single-file + media packs).
- **`POST /v1/images/edits` forwards `lora_paths`/`lora_scales`/`lora_path`/`lora_scale`** (#268): the multipart rebuild used to drop them silently, contradicting the "unhonored = NAMED 400" rule. Array forms pass as raw JSON; `parseLoraFields` owns the 400.
- **A load failure crosses the inference thread by NAME** (`req.error_name` → `ModelRegistry.loadErrorFromName`): preflight refusals → `error.InsufficientMemory` → named 503 + entry reset to `.unloaded`; everything else `Model load failed: <name>` (#144).
- **Image placeholder tokens are identical across images, so state AT/AFTER media is keyed on the PIXELS, state BEFORE it is not** (`vision_key` + `media_start` on every RAM entry; vision entries never spill to SSD): equal keys restore through placeholder rows, different keys cap the match before the earliest media row. A restored prefix hands its placeholder count to the splice (`vision_splice_offset`).
- **Thread lifecycle**: detach every per-connection `std.Thread` immediately; on teardown drain conn threads (`active_conn_threads` + `cancelAllInFlight`) BEFORE `scheduler.deinit`.
- **An adopted spec cache has ONE owner at a time** (#266): `Generator.initWithOptions` frees the restored MTP/DFlash caches on its own error paths, so `runPrefill` clears its locals BEFORE the call — `MtpCacheRef`/`DflashCtx` hold the KVCache BY VALUE and a second errdefer deinit is a SIGSEGV in `freeKVEntry`. Scan-pinned.
- **Sleep inhibition follows the inference-thread wait** (#251): release before `queue_cond.wait`, acquire after wake and before startup load. Guard: `tests/test_sleep_inhibit.sh`.
- **The hot-cache budget FOLLOWS residency (#364)**: `scheduler.reviseHotCacheBudgets` re-clamps every resident model after any load/unload (own resident bytes excluded, `BudgetRevise`), `HotPrefixCache.setBudget` evicts down; the OS returns freed pages LAZILY, so the revise repeats before each prefill for 10 s after an unload. Guard: `tests/test_prefix_cache_budget_revisit.sh`.
- **The hot-cache byte budget is CLAMPED at load to what the weights leave under the GPU ceiling and is a HARD cap** (`clampedPrefixCacheMem` via `prefix_cache_mem_resolver`; requested 0 bounded too, never 0 = "no cap"). A RAM restore REFCOUNT-SHARES its buffers; `active_mem` already holds the resident entry. App: `summarizeCrash` skips `> "` lines.
- **An oversized hot-cache candidate is TRIMMED to the longest restorable prefix that fits, never flat-declined** (#330): `trimLenForBudget` picks the point (hybrids cut at a checkpoint incl. its bytes, floor 256) and `KVCacheSnapshot.trimmedCopy` is a REAL slice+copy (`truncate` is offset-only). Spec snaps drop.
- **The replace path sheds inherited checkpoints before evicting its own entry, and commit owns `ssm_cps` on EVERY outcome** (#330 adjacent): near-budget hybrids thrashed commit→evict-all→cold, and the scheduler's catch-arm free after a failed commit was a double free.
- **Every long-context mechanism of PR #363 is gated by ONE predicate, `ModelConfig.longCtxGated()` (qwen4_exp)**: KV reservation, pad-waste kv length, checkpoint thinning, admission terms, chunk-sizer changes, the wired-limit floor, SSD-first. Other archs keep their previous bytes; never hand-roll it at a site (`HotPrefixCache.cp_thin`, `DiskTier.ssd_first` mirror it once at wiring).
- **An MLX failure is CATCHABLE; mlx-c's DEFAULT handler `exit(-1)` is what killed us** (#353, `mlx.installErrorHandler` once in `main()`, `MLX_SERVE_MLX_ERROR_LATCH=0`): `checkError` per prefill chunk (before it is snapshotted or persisted) + `checkErrorDecode` per tick; a latched error never 200s; streaming shares `mapGenerationError` with non-streaming on all four surfaces. Guard: `tests/test_mlx_error_recovery.sh`.
- **A long prefill EVICTS the hot cache to be admitted, on the INFERENCE thread** (#353, `evictLruToAdmit`, `fits` re-asked after every eviction, never the restored entry): the guard credits only PROVABLY reclaimable bytes (residency − largest entry, via published digests, never the cache pointer), refuses by NAME (`PrefillDoesNotFit` → 400), and DEFERS a warm prompt to the inference thread's `WarmPrefix` bill: a CHECKED-OUT restore credits the rows it HOLDS (min matched/capacity/reserved; the ALL-OR-NOTHING gate billed a growing chain COLD), a grow bills ONE eval window of old buffers (`grow_coexist_bytes`). ONE `[admission]` line.
- **Past 32k a request RESERVES its KV capacity up front** (`KVCache.reservedTokens` = prompt + `min(max_tokens, RESERVE_GEN_HEADROOM 8192)` + one chunk; `MLX_SERVE_KV_RESERVE=0`): a grow is not in place (7.75 GB coexistence at 458k). The guard bills exactly this, clamps `max_tokens` FIRST (the omitted sentinel is maxInt/4) and bills retained SSM checkpoints. `--max-tokens N` in serve mode is the omitted-field default.
- **On qwen4_exp the prefill width is per-REQUEST and re-chosen per CHUNK** (`chooseRequestPrefillChunk` = widest rung the admission estimator affords, re-asked AFTER the eviction pass and the re-ask WINS both ways; `adaptivePrefillWidth`: step down at once, widen one rung at 1.25x after two probes, never after a step-down). The tail-merge bound scales only there; hook PRESENCE is not the gate.
- **A load-time bill runs INSIDE `Scheduler.init`, so there is no scheduler to ask**: KV width from `configuredKvQuant()`; the hot-cache budget bills the STATIC ceiling, the context from `resolvedContextForLoad` with the CONSTANT `CTX_SIZING_CACHE_RESERVE` (never the ask: a 60GB ask advertised 870 tokens), the clamp reserves the ladder FLOOR, the chunk sizer never reads the cache ask. All gated.
- **SSD-first (qwen4_exp + a disk tier, `prefix_cache.ssdFirstActive`)**: RAM floors at ONE session, `--prefix-cache-mem` = the IDLE allowance (hard cap); spill and EVICT are two decisions (`PersistOutcome`: only `.persisted` + an agreeing index + landed files license discarding RAM); writes ride `kv_disk_writer.zig` (FIFO, `meta.json` last, epoch fence at the ONE removal site, failures attributed to their entry); per-chunk write-through; a diverging turn hard-links the donor's LANDED chunks (v6); a full-prefix hit CHECKS the entry OUT so the first append donates. Levers + design: `docs/reference.md`.
- **A checkout is a PROMISE until the append DONATES** (`donateCheckout` right before `Generator.initWithOptions`, below every refusal; `releaseCheckout` hands an undonated entry back intact): releasing at restore time threw away a 364k session for a request refused before its first forward. A 0-token outcome is not a restore (it clears `last_restored_used`), and the lien test weighs the DELIVERABLE share (a hybrid is clamped to its highest checkpoint at or below the match).
- **Checkpoint retention thins the INTERIOR with a dense newest quarter; a trim bills only what a shed KEEPS** (#330, `spanPreservingDropIndex`, `ThinPolicy`; ungated = each site's previous policy): drop-oldest end-anchored the survivors and a 383k entry flat-declined. `TrimDecline` names the outcome.
- **The batched pad-waste cap read `cache.step`, which is 0 forever on a linear-layer-0 trunk** (`KVCache.kvLenForBatching` → `Slot.batchKvLen`; gated, other hybrids keep the dead cap pending a multi-stream measurement).

- **A decline is not a commit — make it observable** (`CommitStatus` through the wrapper chain, 2026-09-07): a cancel-salvage decline used to exit via plain return and the scheduler logged `committed N/M` for it. The cancel-commit log fires ONLY on `.ok`; declines carry their `TrimDecline` reason, disk lookup misses log at debug.
- **An entry's pixel key applies only to rows it covers** (`eff_vision_key` in `commitWithMediaState`): a committed range ending before the media position is PURE TEXT — null key, shareable across vision variants; a trim below the boundary re-keys the same way and drops the (stale) replace target. The cancel path passes the boundary RAW (the old `start < len → null` + kept key was the boundary-less cross-key poison).
- **Checkpoint inheritance and thinning obey the media boundary too** (`bestCheckpointDonor` takes `eff_media_start`, `boundaryCheckpointIndex` feeds `spanPreservingDropIndex`'s `protect`): a key-mismatched entry donates checkpoints at or below `min(entry.media_start, commit media_start)`, and no retention policy drops the highest checkpoint under a boundary.
- **The SSD tier serves the pre-media text prefix for media requests** (capped at the request's `media_start`): disk entries are text-only by construction, so the blanket `vision_key != 0 → break :disk` starved image-bearing agent turns of the whole tier. The cap is load-bearing: a row at/after the media position must come from the vision splice, never a text prefix.
- **Disk checkpoints come off the TOP of the flush budget** (share ≤ `max_flush_bytes/2`, eligibility = kv_TARGET not the flushed kv_len; SSD-first writes them outside the budget): chunk-first budgeting starved cps to zero on chunk-heavy turns (544 MB of chunks vs a 512 MB cap — every Sep-4 entry landed KV-only, unrestorable). A cp beyond this flush's chunks is written anyway (position-keyed, immutable) and becomes restorable when a later flush extends kv_len past it.
- **The DISK hybrid arm ranks entries by restorable checkpoint** (`bestHybridMatch`), mirroring the RAM tier's #312: a longer raw match whose checkpoints sit past the divergence restores nothing and must not shadow a shorter entry with a higher cp (it did exactly that the moment a growing conversation entry's usable length passed the old entry's).
- **A RAM budget decline spills the candidate to SSD** (`spillDeclinedToDisk`, text candidates only): the budget declines RETENTION, not value — a byte-capped spill continues from the next commit of the same conversation (chunks dedupe by token range). The spill's flush budget is floored at `DECLINE_SPILL_FLUSH_FLOOR` (4 GB): the client is already gone and the per-flush cap prices a LIVE request's stall (live: a 512 MB cap banked 13k tokens/retry while ~35k were recomputed).
- **Serial ≠ exclusive**: module-owned decode state needs admission single-flight — `scheduler.admitPendingTick` keyed on `modelExclusiveDecode` (NEVER `!modelBatchable`); held slots stay in `pending` so keepalives flow.
- **`--model-dir` is REPEATABLE**: `discoverModelsMany` merges roots FIRST-WINS; app `ModelRoots` is the ONE answer — READS go through `readRoots`, writes/cancel/DELETE stay on `ownedRoots`. Guards: `tests/test_multi_model_dir.sh`, `ModelRootsTests`.
- **A KV bill is per CACHING LAYER at the arch's OWN K and V widths** (`ModelConfig.kvBytesPerToken` fed by `attnCacheLayerCount`): linear layers hold fixed state; MLA K is wider than V. Auto-context sizer + `prefillMemoryNeeded` read the ONE helper.
- **The STORED width and the SCORED width are two parameters** (`prefillMemoryNeeded(… hdim, score_hdim …)`): score term reads `prefillScoreHeadDim()`, dequant transient reads stored `head_dim`. One value for both billed MLA's 192-wide scores at ZERO.
- **An MLP envelope is not a prefill working set — the arch's OWN streams are the missing term** (`prefillStreamBytesPerToken`): linear-attention hybrids hold a chunk-wide q/k/v stream in the layers the cadence eval has not yet released (`min(linear_layers, cadence + 1)`, since the conv_state tail is an owned copy); MoE replicates the hidden stream `top_k` times. `max(3 x mlp, mlp + fwd x stream)` — the max is a FLOOR.
- **A lazily copied side-channel state is not in the residual's graph** (`conv1dWithCache` tail, #366): a slice of the chunk-wide `conv_input` pinned 3 GB per 4096-chunk; the owned copy must be NAMED in the cadence eval vector (`evalCadencePoint`) or it stays lazy and still holds the parent. Guard: the retention + peak-memory tests.
- **The qwen4 MTP head's KV is dense by default; `--mtp-head-kv-quant` opts it into `--kv-quant`** (`qwen4MtpHeadKvConfig`, retained on the cold-load `LoadRequest`): acceptance within noise of dense 4k-128k with the flag, 960 B/token saved, the head KV BILLED at its effective width either way (`mtpHeadKvBytesPerToken`); a spec sidecar under another scheme is declined at restore and rewritten on the next commit (`specWorkPending`). Below ~2048 head rows the quantized cache misses the fused kernels (a short-prompt cost), which is why it is opt-in.
- **A bill follows the buffer it billed**: the QSA raw keys are a 32-row ring (#381) billed once per slot (`qsaRingBytes`), only the pooled bank is per token; under-billing is a Metal OOM, so a bill goes down only where the bytes are gone.
- **The chunk-independent part is a RUNTIME floor** (`PREFILL_RUNTIME_FLOOR_BYTES`, both consumers + dsv4 sibling): dense bf16 pays it too; `--prefill-chunk` buys it back.
- **The prefill CHUNK is a machine decision** (`resolvePrefillChunk` + `ModelConfig.pinned_prefill_chunk`): ladder 8192→512 takes the widest rung whose reserve ≤ a QUARTER of the serving budget; `--prefill-chunk` wins. Vision prefill chunks like text (#197, `vision_splice_offset`; `MLX_SERVE_VISION_CHUNKED=0`).
- **A `layer_block_types` hybrid was billed KV for every layer** (`attnCacheLayerCount`): lfm2/nemotron_h set `layer_types`/`hybrid_override_pattern`, not `full_attention_interval`. Only `.attention` blocks reach `ctx.cache`.
- **A gate that runs BEFORE the estimator that knows better IS the estimator** (#126): eviction gate + preflight + COMMIT read ONE estimator (`gen.estimatePeakResidentBytes` → `gateEstimateBytes`); refusals name `--max-resident-mem`.
- **A refusal quotes the number it COMPARED** (`scheduler.loadRequirementBytes` is the ONE number, scan-pinned across text + media sites); `loadErrorFromName` keeps BOTH InsufficientMemory/OutOfMemory as a memory 503. App: every gen pane's Show log opens the Server Log window.
- **A staged bill is per-STAGE**: transient allowance belongs to the allocating stage; a stage's weights are what it KEEPS (`h3DitResidentBytes`).
- **A residency bill is a PLAN** (`stagedPeakBytes`): sum-of-directory only for hold-everything backends; LTX = sum − spare variant + out-of-dir TE (`ltxPeakBytes`).
- **A video response is ONE JSON body, so the transport cap bills DELIVERED frames** (#283, `gen.videoRgbTransportReason`, 768 MB raw RGB = the app's `maxFramePayloadBytes`; chained H3 windows died `WriteFailed` because `chain_windows` was never billed). Both video admissions refuse by NAME before a denoise step; the app bills windows into `frameOptions`.
- **A long job's abort must not depend on the RESPONSE SHAPE** (`gen_sse.StreamCtx.stream`): probe is `Conn.peerClosed`; `stream:false` no-ops the event cb; cancel arms report `error.Cancelled`.
- **Non-text model on a text surface must 400 BEFORE prefill** (`textGenRejectReason`, arch_hint peek + post-load). New surface → `isTextGenRoute`; new modality → `modalityFromType`.
- **An arg loop with no else branch is a silent flag eater** (`cli.classifyUnparsedArg`). Every `--flag` any script or `ServerOptions.toCLIArgs` passes must be in main.zig's match list. Related settings travel as ONE value (`server.PldDefaults`); guard `tests/test_headless_spec_flags.sh`.
- **A client-supplied PATH is proven on OUR side of the mlx boundary** (`lora.loadFile` stat → 400; an MLX error KILLS the process).
- **Hand-written error text is not JSON**: escape at the SINK (`gen_sse.jsonEscapeMessage`), truncate on a UTF-8 boundary.
- **Endpoint EXISTENCE must not depend on model state, and the 404 is answered BEFORE the model is resolved** (a typo'd `/v1/load` once cost 2m42s + 121 GB): gate is unconditional, above every load, below the LAN proxy, ONE copy; `ROUTE_PATHS` scan-pinned. Guard: `tests/test_route_404_no_load.sh`.
- **Hot-cache eviction is WORKLOAD-fair (#378)**: entries carry `cache_key` (`server.requestCacheKey`: `prompt_cache_key` > `metadata.user_id` > system-prompt hash > 0 anonymous); `lruIndexExcluding` evicts LRU within the key holding the most entries (incoming counted), ties = plain LRU. Matching stays by prefix; SSD-first spill order untouched. Guard: `tests/test_prefix_cache_workloads.sh`.
- **Per-model settings ride `~/.mlx-serve/model-settings.json` (#269) and are stamped at BOTH load construction sites** (`scheduler.applyModelSettings`; `doLoadOnInferenceThread` resolves them ONCE over the flags): the effective manual context is `server.manualContext(config)`, the KV width `configuredKvQuantFor(config)`, never `server_config.max_context_size` / `configuredKvQuant()` directly. Guard: `tests/test_model_settings.sh`.
- **A launch flag that shapes a LOAD must be retained on the Scheduler**: `ensureLoaded`'s cold-load `LoadRequest` is a SECOND construction site (prefix-cache, MTP, llama, drafter, ssd all bit). `--drafter <path>` is the exception (`coldLoadDrafterDir`). Guards: source scan + `tests/test_cold_load_launch_flags.sh`.
- **A cache derived from a MODEL is per-model** (JSON grammar token→bytes table was a process singleton; other models masked with a foreign vocab): lives on `LoadedModel` (`grammarTokenBytes`). Guard: `tests/test_json_mode_multi_model.sh`.
- **The grammar mask never walks the whole vocabulary** (#380, `token_mask.buildMask`): structural states probe only tokens whose first TWO bytes are legal (`by_first` + `second_off` under `allowedBytes`), a string body admits every quote-free token by length (`Grammar.stringBodyRoom`). `nextConstrained` samples LAZILY and builds the next forward through `lazyForward` before realizing the token. Bar = oracle test over a full document + byte-identical greedy.
- **Every grammar state must have a legal byte** — an all-false mask is `-inf` everywhere and argmax picks id 0. `stepObject` rejects `,` when `additionalProperties:false` and all seen.
- **Free JSON whitespace is CAPPED per run** (`json_grammar.MAX_FREE_WS` 16, before and after the root too): a masked model whose real argmax is off-schema idles on `\n` forever and the loop guard cut it as `length` with empty content (27B, thinking + enum schema, hot-cache hit). Past the cap only structure is legal.
- **Every schema-mask surface uses the same policy** (`schemaMasksThinking`, #331): defer only across bare `<think>` + atomic `</think>` with no finite response-side reasoning budget; otherwise keep thinking off + token-0 masking. Tools present = no mask. Guard: `tests/test_json_schema_thinking.sh`.
- **A page that must render on an EMPTY server can't render FROM a model**: `GET /` renders above resolution, fetches `/v1/models` client-side; `index.html` is a std.fmt FORMAT string (CSS/JS injected as `{s}`). Guards: `tests/test_index_page.sh`, `tests/html_console_test.mjs`.
- **Console**: mic ONLY in `listening`; replies via `speakableChunks`; one composer, Recents in localStorage (`storableTurns` strips images); markdown from ESCAPED input; no sampling knobs. Media is ASKED for: ONE gen per turn, tool `model` enum == `editableIds`.
- **A stream and a non-stream answer must be the SAME BYTES; leading whitespace is the one thing a stream may withhold** (`chat.streamContentLead`): suppress only an ENTIRELY-whitespace lead chunk and retire its pending logprob entry. All four content emitters, both surfaces.
- **Three more stream/non-stream edges agree**: a spent reasoning budget WITHHOLDS the rest of the thought (never re-routes it to content); a non-stream tool-call reply carries the pre-markup text (`visibleToolPreamble`, both surfaces); a non-stream client disconnect reports `client_disconnect`, never `length` (`nonStreamFinishReason`); a stop sequence cuts at its INDEX on every stream surface (`stopSequenceCut`); request ints clamp (`parseRequestSeed`: negative = unseeded, `clampJsonI32`).
- **A client cannot time our own stream**: use the final chunk's server `timings`, summed across rounds; `stream_options.include_usage` gates it. **The `include_usage` chunk ships `"choices": []`** (`sendSSEUsageChunk`) — the ending appears on exactly ONE chunk. Guard: `tests/test_loop_stop_signal.sh` [2][4].
- **Media on a tower-less model is refused by NAME** (`server.mediaRejectReason`, all three surfaces, before prefill): `--no-vision` parsed the image, dropped it and answered "Sky" for a house with a 200. Guard: `tests/test_qwen4_exp.sh` [11].
- **A tool-response turn can render under the USER marker** (ChatML wraps each tool RUN in `<|im_start|>user`): counting user-ROLE messages misplaces the media insert. `server.resolvedUserMarkersAfter` reads the RENDERED prompt's marker total against role counts.
- **QSA GATHERS selected blocks, never a dense `[S, kv]` mask** (prefill `gatherQsa256`; verify 2..15 = the fused split-K `qsaSparseAttn` on QUANTIZED KV, a dense cache or `MLX_SERVE_QSA_ATTN_KERNEL=0` = the union gather, never the prefill kernel; decode `qsaDecodeGatherAttn`; batched slots + kv <= 8192 keep the dense mask). Selection = the exact radix-select kernel `msv_qsa_select` on BOTH arms. Indexer history: ONE copy per (slot ∪ entry), the newest snap VIEWS the live buffer at commit (`handoffQsaHistoryToLatest`), none after restore = MISS; the f32 score bank is billed.
- **The QSA gather rides NAX cooperative tensors at prefill, gated on its OWN predicate** (`qsaNaxEligible`: G17 + macOS 26.3 (not 26.2) + bf16 + hd 256 + gqa 12 + q_len >= 16; default on, `MLX_SERVE_QSA_NAX=0` restores `msv_attn_qsa256`): the bf16 MMA rounds softmax weights, TWO bf16 terms match stock's error (one is worse, three slower). Not bit-identical: bar = per-element error vs float64 no worse than stock (`tests/qsa_nax_precision.py`), never byte identity.
- **The exact QSA select at decode widths is SPLIT across 16 threadgroups per row** (`msv_qsa_select` slice + merge, `MLX_SERVE_QSA_SELECT_SPLIT=0` restores one threadgroup per row): one threadgroup walking 215k scores four times is latency-bound (0.72 → 0.26 ms/layer), and the merge is exact because the order is total (ordinal, then lowest ORIGINAL index). Prefill widths (rows ≥ 16) keep the single kernel; a divergent `simd_sum` is undefined, reductions stay in uniform loops.

- **The QSA indexer score sheet is ONE NAX kernel on qwen4_exp** (`msv_qsa_score`, `MLX_SERVE_QSA_SCORE_FUSED=0` restores `astype→matmul→relu→sum_axis`): bf16 operands make every product exact, so 8 ascending `matmul2d(16,32,16)` k-steps + an ascending f32 head sum are BIT-IDENTICAL to the stock tf32 GEMM chain. The f32 score bank is never built under it; forward, chunker and bill read `qsaScoreFusedActiveFor(batch, n_idx, idx_hd)`, never the env.
- **A cache keyed on a POINTER is invalidated by an ATOMIC MARK, never an off-thread free** (`QsaPooledRope`/`markQsaPooledRopeStale`): `Slot.deinit` runs on conn threads, so it stores one word; the inference thread's cleanup drain calls `resetQsaPooledRope` before `s.deinit()`.
- **A field DISPATCH reads must be readable from every body SHAPE**: `parseModelFromRequest(body, content_type)` feeds dispatch AND `lanShareDenial`.
- **A content array's text parts JOIN in order — never last/first-wins** (`server.joinedTextParts`, all three parse sites; issue #195 opencode plan mode).
- **A header PARAMETER lookup keys on the parameter at a boundary** (`name=` also matches inside `filename=`). **A body log fine for JSON is not fine for BINARY** (`bodyIsText`).
- **Auto-context bills KV at the CONFIGURED width and activations ONCE** (`kvBytesPerTokenAtBits` + `prefillTransientReserve`, shared with the admission guard, scan-pinned): the prefill transient is flat in prompt length, keyed on `--prefill-chunk`. `prefillMemoryNeeded` UNDER-bills quantized prefill ~22% (known).
- **Auto-context is PINNED at load** (`pinAutoContext`); 85% margin on the MEMORY ceiling only; ask `getEffectiveContextLength`, never `server_config.max_context_size`.
- **GPU memory ceiling must see EXTERNAL pressure** (`currentGpuMemoryCeiling`); Metal OOM is UNCATCHABLE (libllama frames are a red herring); embedded-engine requests are EXEMPT from MLX guards (`mlxMemoryGuardApplies`).
- **The prefill guard bills what the ARCH reads**: `prefillAttnKeys(seq)`, `prefillFfnWidth` from the DECLARED config (MoE = top_k × moe_intermediate + shared), chunk term arch-owned (`server.dsv4PrefillMemoryNeeded`); all scan-pinned.
- **Metal at the working-set edge returns ZEROS before it aborts**: all-zero logits from healthy inputs = MEMORY symptom, not math.
- **A weight outside every warmup forward is still LAZY at serve time**: force-eval at init (`appendWeightArrays`), budget-gate on LOGICAL bytes (`dsparkFitsBudget`); DSpark is opt-in `--dspark`.
- **`--timeout` is a STALL timeout** (seconds without a new token); `toolCallFinishReason` preserves "length" through tool parse on all four surfaces; `outputBudgetGuidance` only when tight (<12288).
- **Ownership by PROVENANCE, never content**: `{slice, owned}` returns, never free-unless-equals-literal.
- **LAN robustness**: per-INTERFACE dns_sd callbacks, loopback-first fetches (Local Network privacy blackholes LAN SYNs), eviction only via `attemptKnown` after `PEER_DROP_FAILS`, raw-body id compares canonicalize `\/`.
- **@peer proxying is bounded by the TUNNEL MARKER**: any direct client gets ONE hop; `X-MLX-LAN` requests never proxy again (`isTunneledRequest` at gate AND dispatch). List filters key on the discovery fetch's marker; `lan_peer` entries never re-export; self-fetch → `error.SelfFetch`.
- **A llama session trim is FALLIBLE on recurrent/hybrid GGUFs** (#286 + #287; `mlx_llama_session_trim` returns 1 = cleared): `llama_memory_seq_rm` refuses a partial tail past the recurrent snapshot window and mutates NOTHING — a trimmed mirror over an untrimmed KV served the PREVIOUS request's tool calls. Cold-prefill on refusal.
- **A READY model never advertises LESS capability than its stub** (`readyHasChat` counts embedded engines; app `lanAdvertises` tolerates empty caps).
- **Default bind is 0.0.0.0 and serve mode WARNS** (`server.shouldWarnOpenBind`); flips to 127.0.0.1 in a future release. The app always passes `--host` explicitly.
- **A reload FREES the CPU state `unloadResident` retains, so the registry mutex alone does not make a read of it safe**: `config`/`chat_config`/`tokenizer`/`token_bytes`/`tokenize_cache` are freed off-mutex while the entry is `.loading` (`releaseRetainedCpuState`, asserted). A reader holding no refcount takes the mutex AND skips them while `.loading` — not `== .ready`, an unloaded entry keeps them by contract.
- **A status route must never reach `ensureLoaded`**: `GET /props` cold-loaded the model, so a 3s tray poll retook the memory idle eviction had just handed back. Nothing resident → answer from the counters (`handlePropsNoModel`).
- **An embedding SUB-BATCH is its own forward** (`computeEmbeddingsBatch` resets the cache before every sub-batch, not the request): a decoder-arch embedder (Qwen3-Embedding) forwards through the KV cache, so a later sub-batch attended to the earlier rows and answered wrong vectors. Guard: `tests/test_embeddings.sh` [4c].

### Engine: KV, spec-decode, kernels, MLX (→ docs/gotchas/engine-mlx.md)

- **The prefill-chunk cap reads the SCORE width** (`ModelConfig.prefillScoreHeadDim`) — the `<=128` fused-SDPA early-out otherwise exempts MLA; hd-256 policy branches key on 256 EXACTLY.
- **On NAX the stock sdpa is the hd-256 kernel** (`naxSdpaPreferred`, default = NAX available, `MLX_SERVE_NAX_SDPA=0|1`): `msv_attn_p256`'s causal arm declines, stock fallthroughs pass `force_fused` ONLY where mlx has a fused kernel (`sdpaForceFusedFor`: hd 256, > 8 rows, q <= k). Band arm stays ours; hd 512 declined. M5 A/B still owed.
- **MLX sdpa has a WIDTH WALL at hd 256** (vector kernel serves `q_len*gqa <= 32`): dense causal q 6..9 ride `splitCausalSdpa` (split at row 5, byte-identical; `MLX_SERVE_SDPA_SPLIT=0`; +4..9%). `--mtp-depth` is a CAP — force verify width via PLD draft-len in A/Bs.
- **Fused decode QK-norm+RoPE** (`fusedQkNormRope`, laguna, default ON, `MLX_SERVE_QK_NORM_ROPE_FUSED=0`): bit-identical; cos/sin from probe rows (`ropeAngleRow`); live paired A/B is the bar.
- **Decode-only dense-attention requant** (`--decode-attn-quant`, default ON, LOSSY): side copies at decode AND spec-verify (same weights); prefill dense; tail layers nvfp4-g16 (`attnDqFor`). A/B per newly-adopted dense arch.
- **MoE decode down-path tail is ONE dispatch** (`gatherQmvDownReduce`, default ON, bit-identical, `MLX_SERVE_MOE_DOWN_REDUCE_FUSED=0`). **lm_head prune is OPT-IN** (`MLX_SERVE_LMHEAD_PRUNE=1`, `ForwardCtx.argmax_only`). Interleaved A/B trick: `presence_penalty: 1e-9` as a per-request lever.
- **Every qwen3.5/3.6/3.8 checkpoint is hd 256** — read the CHECKPOINT before porting an hd-128 kernel.
- **A reference probe with SYNTHETIC dtypes proves the reference's SEMANTICS, not the checkpoint**; dtype-gate any `mlx_array_data_float32` load-time read.
- **KV invalidation** after tool calls + pad-only gens. Sliding-window layers keep the full BUFFER; the VIEW slices to the reachable tail.
- **A sliding layer's view is sized by the whole BLOCK** (`transformer.slidingViewFor`, `window + q_len - 1`, `MLX_SERVE_SLIDING_BLOCK_TRIM=0`): every consumer derives offset RELATIVELY; the fused hd-256 band kernel declines the trim keyed on `FUSED256_MIN_Q_LEN`. Guard: `tests/test_sliding_window_trim.sh`.
- **Hot-cache restore ALWAYS clamps**: unconditional `truncate(final_len)` — restored offset = MATCHED length.
- **A failed restore hands back an EMPTY cache** (`errdefer` around the bind in the lookup): the caller reads any error as "no match" and cold-prefills the whole prompt, so a half-bound KV held the prefix twice. **Every eviction loop needs a no-progress exit** (`evictOneLruProgress`): checked-out entries are unevictable and the commit loops spun the inference thread forever. The hybrid DISK arm adopts the spec sidecar of the entry it RESTORED (`hm.idx`), honours `poisoned`, and reads manifest scalars through bounded `jsonInt`.
- **Slice-born weights into gather_qmm/quantized_matmul are `mlx_contiguous`-materialized at load** (`splitPackedGateUp`) — lazy views compute wrong at scale.
- **SSM/hybrid**: init checks `ssm_state.ctx == null` NOT `!initialized`; param-free RMS norm passes `ones()`; Nemotron dt clip = only `time_step_limit`; PLD snapshots need per-FIELD null guards.
- **Spec verify invariant** (all drafters): `cache.step = prompt_len + emitted`, t1 NOT in cache on entry, verify input `[t1, draft…]`, partial-accept correction from ORIGINAL `verify_logits[accepted]`.
- **A block decoder checks its ENTRY token before drafting** (`generate.tokenStops` in all five: MTP/DFlash/PLD/drafter/DSpark): an EOS `t1` bought a full draft chain + verify, and the realized-vs-emitted divergence tripped the pad-only commit guard so an 88k prefix was not committed (`commitDeclinesPadOnly`: only an ALL-pad generation declines). A cancel mid batched tick still RECORDS the row the forward wrote (`batchedTickAction`). `[short-gen]` names the ids of any <= 2-token answer.
- **The token budget is a PRE-COMMIT invariant in EVERY block decoder**: cap accepted drafts before any mutation; the scheduler publishes every block through ONE incremental `+= 1` loop.
- **The batched-decode gate reads DISPATCH, not ARMED flags** (`scheduler.slotTicksRegular` asks `specTickMode` + `gen.spec_disabled_runtime`): a PLD-armed slot that runtime-disabled itself lost batching forever. A solo slot never reaches the batched path, so PLD's re-enable still runs.
- **A batched MTP verify is ONE `[N, S]` trunk forward, capped at 7 rows off-NAX** (`mtpGroupRowCap`; 8 rows leave the split-K lane for stock kernels, 2x slower): pairs at depth 2, triples at depth 1 (`mtpSubGroupSize`, `Generator.mtp_group_cap`); four or more MTP slots decode PLAIN with hidden capture (`mtp_plain_tick`, `mtpCrowdThreshold`) and resume speculating when the group thins. `MLX_SERVE_MTP_BATCHED=0` restores solo rounds.
- **Concurrent MTP requests draft and verify as ONE group** (`mtp_group_planner.zig`, default ON, `MLX_SERVE_MTP_GROUP_PLANNER=0` or per-request `enable_batch_mtp:false`): a planner prices each width per row from a learned mean round cost and falls to plain batched decode (width 0) when a verify round cannot beat it; the shared-hardware verify kernels are G17-only (`verifySharedHardware`). M5 Max: +19..30% at N=4. M4 Max Flash Next: N=1 and N=4 flat (planner picks width 0), N=2 at 16k +17%; `[mtp-planner]` names the choice.
- **The qwen4 MTP head's per-request state is a `Qwen4MtpState` swapped onto the module** (`qwen4MtpActivate` before EVERY head touch, through `MtpCacheRef`); nothing is module-owned, so MTP slots are no longer exclusive. Its verify rows are expert bytes, so a batched verify measured no better than solo rounds and stays opt-in (`MLX_SERVE_MTP_BATCHED_QWEN4`): rounds stay solo, two interleave, three or more go plain (`mtpRoundsStaySolo`).
- **Spec dispatch discipline**: all four surfaces × stream/non-stream wire `use_*`; MTP default = `server.defaultEnableMtp` ONLY (MoE OFF, `--mtp` flips); engagement COUNTS in tests; priority DFlash > MTP > gemma drafter > PLD (`--no-drafter` returns MTP) via ONE `server.requestSpecModes`; logprobs>0 + grammar disable spec; tools disable NOTHING.
- **DFlash is a METHOD, keyed on the CONFIG CONTRACT** (`block_size` + `mask_token_id` + `target_layer_ids`; `--drafter` is the ONE flag): drafts = `block_size−1` from ONE assistant forward, anchor row DROPPED; context = trunk `hidden_states[i+1]` via `capture_layers`; block K/V never cached; rollback = offset-only truncate. Gates consult `lm.dflash` beside `lm.drafter`.
- **DFlash2 = v1 + selector + convs, keyed on NESTED `dflash_config`, z-lab naming**: encoder `fc`/`hidden_norm` (or `encoder.*`, probed); selector codebooks are SUFFIX-LESS bf16 gather tables, NEVER quantized; bare `model_type` "qwen3", so discovery classifies by contract (`peekConfig`). Base_kernel axes `[prepare|finish, tap, channel]`. `MLX_SERVE_DFLASH_SELECTOR=0` restores v1.
- **DSpark = DFlash + Markov chain, config split across two objects** (`dflash.Contract`, `MarkovHead`): `block_size` at ROOT, rest under `dflash_config`; flat `rope_theta`; `rope_is_neox_style:false` = MLX `traditional=true`. Markov head adds `w2(w1[prev])` per position (`MLX_SERVE_DFLASH_MARKOV=0` halves acceptance), `w1` DENSE, only `vanilla` ported.
- **Hybrid forward is capture-capable; a partial accept rolls the CONV state back** (`supportsLayerCapture` includes hybrids; `ssmRollbackFromCapture` takes the verify width). `nextDflash` decides `hybrid_path` separately from `moe_path`: a hybrid's `cache.step` is genuine.
- **DFlash gate's bar is round cost / serial step cost, calibrated on DENSE trunks** (`dflashGateMinimum`, `DFLASH_MOE_GATE_MIN_ACCEPTED_PER_ROUND` 1.8): a sparse target takes an absolute floor after width scaling.
- **The hybrid veto on assistant sidecars belongs to the GEMMA drafter** (`server.archBlocksAssistantSidecar`, ONE predicate across four surfaces) — DSpark on LFM2 otherwise decodes serial with green boot lines.
- **The ngram spec-gate scores the PROMPT; a DFlash drafter is EXEMPT** (its runtime yield gate sees realized acceptance; llmprobe can't send `enable_drafter`). Gemma drafter keeps the gate. Acceptance is a THINKING-MODE property (muse DFlash2 12.5% off vs 47.8% on).
- **A spec sidecar the checkpoint SHIPS is a load-time dependency** (`dflash.resolveInDirDrafter`, `<model_dir>/drafter`): explicit `--drafter` wins, else probed at LOAD; opt-out `--no-drafter`; discovery refuses `*_assistant` standalone. Ship at the width the server quantizes to anyway (8-bit).
- **The spec BLOCK is the verify WIDTH, a MACHINE property** (`dflash.resolveBlockSize` + `wideVerifyLaneAvailable`, `blockCapForMachine` on sysctl brand: M3 Ultra 8, else 5); `--draft-block-size` clamps against CONFIG; yield gate scales calibration by `(block−1)/15`. `DEFAULT_DRAFT_HEAD_BITS` = 0.
- **A prefix-cache restore forwards NO trunk layers, so trunk-derived draft state rides the cache** (`prefix_cache.DflashSnap`, `Entry.mtp`, `restoreSpecSnap`): adopt only on `base + step == matched`; commit dflash only when `absLen() == prompt + generated`; MTP trims to `mtpCommittedLen`. Survives the SSD tier (`spec.safetensors`; a spec salvage failure drops only the spec).
- **A reduction helper that has only seen ONE row cannot reveal an axis bug**: sampling/filter helpers that could take a block use the `_axis` op (`applyTopK` NaN'd on `[m, V]`).
- **A spec sidecar's own weights are a per-round READ — precision is a LEVER**: dense bf16 assistants affine-quantized at load (`MLX_SERVE_DFLASH_QUANT_BITS` 8), packed REPLACING dense; indivisible widths stay dense per WEIGHT. Draft head re-encode (`MLX_SERVE_DFLASH_DRAFT_HEAD_BITS`) default OFF.
- **Hand-typed per-silicon spec tables and boot-probe cost ladders were BOTH wrong**: a ladder can't see ACCEPTANCE, a forward is not a ROUND. Per-silicon rows survive as COLD-START caps keyed on `ane.chipBrandString` (`verifyQmmParitySlack`, `mtp.adaptiveDepthCapForMachine`, each LABELLED in the log); the measured table decides the rest.
- **Round cost is MEASURED per model/width/KV bucket from live SINGLE-CHUNK rounds** (`src/round_cost.zig`, `MLX_SERVE_MTP_COST_TABLE=0` = prior only): EMAs of ms + tok, solo only, cells count at 3, m_lo 5% hysteresis. `tok` is a workload MIXTURE; planning from per-request acceptance LOST. EV plan (`MtpCostSource`): linear between measured widths, prior below the anchor, max(slope, prior) past the widest, `clearlyWorse` (≥20%) floors it.
- **Width trials** (`mtpWidthTrialTarget`, 3-round block per period 16+): m_lo, then m_lo+1 — NEVER m_lo−1. The silicon depth row is a COLD-START cap (`mtp_depth_free`), exceeded only up to the widest TRUSTED width. Trial plans from a scalar (`.m_hi = m_lo + 1` aliased); `[spec-stats]` carries `width_trials= table=`.
- **Round-cost persistence is OPT-IN** (`MLX_SERVE_ROUND_COST_PERSIST=1`; absent or `0` = every boot re-measures, the first MTP requests per bucket run on the prior). When on, the file is keyed on (chip, model, quant, OS build, engine build: `-Dgit-sha` for release, else exe hash + MLX dylib/metallib fingerprints); no cell is ever wiped on serial drift. DFlash/DSpark: `round_cost.WidthChooser` picks block PER ROUND (serial STICKY), OPT-IN `MLX_SERVE_DFLASH_CHOOSER=1`.
- **A round's wall is between round ENDS, so an interleaved prefill chunk lands in it** (`interleaveDecodeTick` drops the round clock too): w2 read 3x w1 and the planner never left w1. The table refuses a width past `IMPLAUSIBLE_STEP` (1.5x/step) of the nearest TRUSTED narrower cell and past `SELF_SPIKE` (3x) of its own mature value, at fold (`table_drops` `i`) AND at load (`restored_dropped`); the load sweep ALSO drops a cell past `IMPLAUSIBLE_WIDER` (1.25x) of its nearest trusted WIDER cell, the only bound that reaches width 1 (#382); `tok` is a workload MIXTURE, so ms/tok plans on the MONOTONE tok envelope (`planTok`).
- **An f32 SCALAR array promotes every bf16 operand it touches** (`mlx_array_new_float` in `scaleTriple`, the PLE gate, the `2·sigmoid` inject closure): one stray scalar widened the whole qwen4 residual stream to f32 (22.0 → 18.4 ms/forward once fixed). Scalars go through `scalarOf(v, dtype)`; `[dtype-trace] residual widened` is the tell.
- **A decode custom kernel can be LATENCY-bound rather than op-bound** (qwen4 fused hc read, `hcReadFused` N/D/U, `MLX_SERVE_HC_FUSED=0`): one lane walking 40 dependent loads costs the same regardless of work, so split-K + unrolled loads beat the 11-op chain. In-situ `MLX_SERVE_DECODE_FWD_UBENCH` is the meter. `hcWrite` DEFERS into the next read.
- **`mx.quantize` packs DENSELY: element i at bit offset `i*bits` of the u32 stream, straddling words at 3/5/6 bits** (#305, `NgramTable.dequantRow`): a `32 / bits` per-word reader is right only where bits divides 32 (a 6-bit pack served noise with fluent text). Every hand-rolled unpack is tested at EVERY width mx.quantize ships.
- **A random read into a cold 32 GB mmap is an SSD fault, serial, and faults on ONE mapping serialize on the VM map lock** (`NgramTable.gather` → `PrefetchPool`, 48 parked `pread` workers, `QWEN4_PLE_PREFETCH=0`); `NgramTable.startWarm` preads the table in the background at load (`MLX_SERVE_NGRAM_WARM=0`).
- **A host read of the TOKEN inside a decode graph build serializes the build with the GPU** (qwen4 PLE at layer 1): `ForwardCtx.ple_defer` hands the graph a zero bf16 leaf and `flushDeferredPle` fills it AFTER the build. BOTH `lazyForward` and the MTP VERIFY build set + flush it (+10% decode; verify lap 6.0 → 2.0 ms). Guard: `qwen4 deferred PLE`.
- **A deferred PLE gather CLAIMS its spec slot at BUILD time** (`pleClaimSpecCapture`): the PLE conv capture later in the SAME forward keys on `entry.spec_ple_len`, unwritten until the flush (and `spec_capture_ssm` is false by then). Miss it and `ssmRollbackFromCapture` silently takes the non-PLE branch on every partial accept.
- **A dependent-kernel cut that REDISTRIBUTES a reduction into every threadgroup loses** (hc read N folded into D/U: 9% SLOWER). qwen4 decode is bytes plus ~840 dependent kernels; experts, GDN proj, shared and lm_head are AT bandwidth (`QWEN4_STANDIN=…`). Sub-2% calls need an IDLE box.
- **The qwen4 residual stream is bf16 like the checkpoint; the f32 fixture is the MATH oracle** (`Transformer.qwen4_stream_f32`, env `QWEN4_STREAM_F32=1`): a tiny random model's logits are chaotic between two bf16 renderings, its streams are not — `qwen4 fixture bf16` pins them at 0.999 against `QWEN4_REF_DTYPE=bf16`.
- **GDN decode is three fused dispatches per layer** (`gdnPreworkFused` S 1..9, `gdnNormGateFused`, `MLX_SERVE_GDN_DECODE_FUSED=0`): S 1..9 bit-identity is SAMPLING (fused `beta` 1 bf16 ulp off `mlx_sigmoid` on rare inputs); a greedy byte flip is legit. −4% qwen4 forward.
- **A routing-independent chain the GPU already OVERLAPS is not a dispatch to fuse**: shared expert as an 11th gather slot −3%, gated shared tail in the down+reduce epilogue null, in_proj fold null, multi-row gather kernels for verify widths +38% worse. Measure the marginal in-situ (`MLX_SERVE_DECODE_FWD_UBENCH_S=<rows>` / `_KV=<prefill>`).
- **A diagnostic env read with `getenv != null` is ARMED by `=0`** (a harness exported `QWEN4_PROFILE_FWD=0` and every MTP arm ran the per-block SYNC profiler): diagnostics go through `diagEnvOn` (absent or `0` = off).
- **Spec acceptance is a PROMPT-TYPE property; measure it per index before touching a round's cost** (`MLX_SERVE_MTP_FORCE_DEPTH=n` + `acc_idx=` on `[mtp-trace]`): qwen4 code 0.91/0.80/0.68, prose 0.65/0.37/0.25, on BOTH head packs. Auto ≈ fixed-2 on prose; MTP stays opt-in.
- **One effective YaRN mscale on every QSA indexer arm.** M-RoPE tables already include it via `fillCosSin(..., yarnMscale)`. Scalar arms apply it through `yarnScaleRotated` / `ropeAtFreqs`+`scalarOf`; never `mlx_fast_rope` scale, never `yarnScaleQK` on the 128-wide indexer, never double-scale M-RoPE.
- **The QSA indexer ropes with the SAME M-RoPE table as attention** (`qsaMask` takes `ctx`; `beginMropeChunk` is ONE preamble): queries from `mrope_cos/sin_cur`, pooled block keys at 3-D block-START positions, both `abs + delta` past the table. `qwen4 fixture vision` pins the mask EXACT.
- **MLX's `use_fallback` has NO fused arm for an hd-256 ARRAY mask**: the dense-mask arm at verify widths rides `splitMaskedSdpa256` (`MLX_SERVE_SDPA_SPLIT=0`); `msv_attn_p256`'s mask arm (`fusedSdpa256Masked`, `MLX_SERVE_QSA_FUSED=0`) is the prefill FALLBACK behind the gather kernel. `QWEN4_STANDIN=attn_qsa` is DENSE causal attention.
- **A per-arch exclusion as a hand-rolled conjunct is a list of ONE**: both sides read `Transformer.ownsModuleDecodeState()` + `scheduler.specInitWiring`; class scan pins every `?*<x>_mod.<Y>` field. Exclusion is per-arch CAPABILITY (`moduleStateSpecRollback`), pinned via error RATIO never bit equality.
- **"Module-owned" is a property of the STATE, not the pointer field** (`module_owned_state_fields` dsv4 vs `module_shared_readonly_fields` qwen4): a read-only module admits and batches freely; only the slot driving a module-owned MTP head is exclusive (`slotExclusiveDecode`). Spec wiring keys on `moduleSpecWiring` (both lists).
- **A decode kernel keyed on `batch*seq == 1` declines every verify row AND every batched slot** (qwen4 hc read/write, GDN prework/norm-gate): the grid carries the rows (`HC_FUSED_MAX_ROWS`/`GDN_FUSED_MAX_ROWS` 16), bit-identical per row. A per-row `gatherQmv` loop for the MoE at S>1 lost.
- **A qwen4 verify row is BYTES, not dispatches** (S=1/2/4 = 16/22/31 ms; halving the op count moves nothing): a second row's own experts are read, a depth-2 round = 2.05 serial forwards, prose accepts ~1.0 — MTP stays opt-in.
- **`--no-mtp` must gate an IN-CHECKPOINT head too** (qwen4's layer loads with the trunk and the `--no-mtp` baseline of `test_mtp_equivalence.sh` ran MTP rounds): `entry.mtp` reads `params.mtp_enabled`, `slotExclusiveDecode` keys on the ENTRY's head. `MTP_FORCE_ENABLE=1` on MoE.
- **The qwen4 MTP head rides the slot's M-RoPE table on image turns** (`qwen4MtpForward(…, mrope_ctx)`: `beginMropeChunk` at its ABSOLUTE `pos_base + seq_offset`, scalar `offset + delta` past the table; its history spans the image rows, so the delta alone was never enough). Bar = `test_qwen4_exp.sh` [7b].
- **A GDN trunk's `KVCache.step` is 0 forever** (it advances on layer 0, a linear layer): the batched tick roped every qwen3_5 decode token at position 0. Batched rope offsets read the slot's `moe_seq_offset`, which the batched tick ADVANCES. Bar: `test_batched_equivalence.sh` forced-N=1 byte-identical, N=2 acquits near-ties (≤0.15 nats).
- **A module-owned forward that ignores `ctx.capture_hidden` hands spec an EMPTY hidden**: publish last-row + all-rows via `skip_lm_head`. **A guard that shapes INIT options does not bind DISPATCH**: tick dispatch through `specTickMode`; `nextPld` self-declines on `!pld_enabled`.
- **A host read inside a layer loop is a GPU BARRIER**: defer non-consumed reads into ONE batched eval (`chunkCrossesBoundary` is pure arithmetic). **A rollback that re-forwards is a SECOND forward**: capture only what verify overwrites (`DsparkAnchors`), truncate the rest by offset.
- **DSpark serves SAMPLED requests via MTP one-hot acceptance** (default ON, `MLX_SERVE_DSV4_DSPARK_STOCH=0`); penalties/grammar/logprobs serial; greedy byte-pinned.
- **MTP head**: delta-encoded norms AUTO-FOLD at load (oQ raw-HF head norms get the `+1` repair); quant re-solved PER WEIGHT; verification is ALWAYS the trunk head; every spec lever kill-switched and bit-identical to off.
- **A sidecar carries weights and NO config, so its quant MODE is solved from GEOMETRY** (`transformer.quantParamsFromGeometry`): affine is the only mode shipping per-group biases, so `loadLinear` takes `.biases` optionally and `qLinearFwd` passes `mode.cstr()`, never a literal `"affine"` (demanding the triple killed every nvfp4/mxfp4/mxfp8 head at load).
- **A dense bf16 MTP head trunk is requantized at load** (`mtp.loadTrunkLinear`, `MLX_SERVE_MTP_HEAD_QUANT_BITS` 4/g64; fc stays bf16 for m5Nax): +10% decode. `test_mtp_equivalence.sh` acquits divergences at serial top-2 gap ≤ 0.15 nats and boots `--prefix-cache-entries 0`.
- **The head's `fc` can ship QUANTIZED** (`MtpModel.fc` is a `QLinear`; `fcMatchesHidden`/`affineParamsFromGeometry` solve dims from packed geometry; axis 0 quantized, axis 1 dense). m5Nax profile still demands bf16 fc → falls to `.generic`.
- **A norm-repair heuristic keyed only on an ANCHOR GAP convicts correct packs** (`mtpNormNeedsRepair` also reads the norm's OWN negative fraction; whole-head 5% bar is NOT per-tensor) — a correct post_attn got a second +1 and acceptance collapsed.
- **No KV snapshots across verify**: rollback = scalar anchors + per-position SSM capture + offset-only `truncate`. **A multi-token forward is not a prefill**: `prefillEvalCadenceApplies` (seq ≥ 32) exempts verify/canvas/batched forwards.
- **INT4 long-greedy divergence is legit**; byte-stable greedy ⇒ no spec + `--kv-quant off/8`. **A prefix-cache HIT is not bit-identical on a HYBRID** (≤0.047 nats) ⇒ `--prefix-cache-entries 0`.
- **Quantized-KV packed reads are kernel-or-DENSE, per WIDTH** (`kvAttnFusedEligible` t_q==1 → `qkvAttnDecodeKernel`; `kvAttnVerifyEligible` t_q 2..8 → `qkvAttnVerifyKernel`; else dense, scan-pinned). Verify kernel OFF on G17 only (`MLX_SERVE_KV_ATTN_VERIFY=1|0`); floor 2048. Guard: `tests/test_kv_quant_fused_equivalence.sh`.
- **A parity loop asserts FINITENESS before it diffs**; additive masks via `mlx_where`. **Threadgroup memory is an OCCUPANCY decision** (≤ ~10 KiB, `qkvDecBlockFor`).
- **kv-quant contract**: attention always reads `KVCache.denseView` on EVERY path (batched included); schemes extend via enum + two switch arms; `HotEntry` records its scheme; quant snapshots carry 6 handles.
- **Hybrid-SSM prefix cache retains ~3.4× what it reports**: lever is ENTRY COUNT (`ramCappedPrefixCacheEntries`). **SSM-checkpoint stride never sub-divides the chunk** (`effectiveSsmCheckpointStride`); dense hd-256 chunk cap 8192, MoE 4096.
- **Hybrid prefix candidates rank by RESTORABLE SSM position, not raw token match** (`findBestRestorableMatch`): a longer match with its first checkpoint past the divergence must not hide an older usable entry. Guard: the moved-image-span fallback test in `prefix_cache.zig`.
- **The always-on SSM snapshot sits `SSM_SNAPSHOT_BACKOFF` (30) tokens BEFORE prompt end** (a snapshot AT the end is unreachable next turn — the llmprobe cache-hit mechanism).
- **hd-256 prefill kernel (`msv_attn_p256`)**: band always fused; plain causal default-on via kv-chunk budget; q_len < 16 ALWAYS declined; guards bill through `prefillHeadDimFused`.
- **A load-time constant table in the WRONG DTYPE silently widens every read** (Laguna mscale f32 = 3.03x): `constTableAs` hands tables back in the activation dtype; bf16 IS the reference on bf16-calibrated checkpoints; bisect with `diagProjBench`. Scan-guarded.
- **Every forward path carries a `[dtype-trace]`**; 1-D f16 tables narrowed at load (`narrowsLoadedF16`, `MLX_SERVE_F16_NARROW_1D=0`); multi-D dense f16 stays.
- **A weight-layout fusion is not output-preserving** (changes which KERNEL runs); fused QKV null → opt-in. **A fusion pays only if it shortens the DEPENDENCY CHAIN** (`fusedAddRmsNorm` negative).
- **Reproducing an MLX op means reproducing its REDUCTION TREE and ACCUMULATOR**; `mlx_compile` on the same math is NOT output-preserving. **JIT vs metallib transcendentals disagree; a 16-bit domain is swept ENTIRELY** (`swigluSigTable`); f32 DECLINED.
- **Fusing gate+up flipped `gatherQmv` into the decode default** (`gatherQmvGateUp`): eligibility = the FUSED KERNEL'S OWN conditions (`useGatherQmvDecode`), never a model_type list.
- **The fused MoE decode kernels take 3-bit as a BYTE TRIPLE, never a 3-word unit** (`mlxserve_qpack`, all five gather kernels): mx.quantize packs 8 values in 3 bytes, and a 32-value unit left half the lanes idle a third of the time (83 us vs 73 for 4-bit on fewer bytes). These kernels are ALU-bound at <= 4 bits, so bytes saved do not show; lane balance does.
- **The MoE down+reduce kernel splits each output row over 8 LANES with the packs hoisted** (`DOWNRED_LPR`, both solo + rows): K is the short MoE intermediate, so one 32-lane `simd_sum` per row cost more than its dot product and K/8 packs over 32 lanes idled a third of them (34 -> 24 us per layer at 2/3/4-bit, 8-bit 43 -> 37). 4 lanes spills the hoist. Reduction order differs from the composed chain: bar = fp32-truth RMS no worse than the chain, never bit identity. A/B kernels INTERLEAVED in one process: separate runs drift 15%.
- **A `metal_kernel` config rebuilt per call is CPU tax — cache keyed by FULL SHAPE** (`ShapeKey`; element-count key collided and killed the server). **A bandwidth bench smaller than a real step measures CACHE.**
- **A lever that pays in another harness may pay for a constraint we don't have** (intra-step async-eval: negative, OFF), so a DEFAULT belongs to the engine that MEASURED it (DSpark confidence gate OFF here). **"Duplicate and read the marginal" is unsound for anything that MUTATES state.**
- **Prefill perf kernels A/B PER ARCH before default-on** (`gdnBlockedEligible`, `prefillDqGemm`); fused-causal hd-256 archs cap auto chunk at `min(base, 4096)`.
- **Verify-qmm plain-SIMD tiles are BITS-templated; SHAPE-gating is the win** (`mixedPlainShapeEnabled`, default 6-bit gs64 `N >= 5120`, `MLX_SERVE_VERIFY_QMM_MIXED_PLAIN=0|1`): 1.10–1.22x on 27B 6-bit; every-shape-on 1.03x (1024-wide K/V lose); 8/5-bit default OFF. Hoist the WORDS, not the pointer; `BITS` needs no cache-key change.
- **A wider spec BLOCK loses off-NAX even when the verify gets cheaper** (27B/M4 Max: tok/s peaks at block 5 while accepted/round climbs to 8; 8 puts novel BELOW serial): `wideVerifyLaneAvailable()` NAX-only, M4 cap 5, `WidthChooser` opt-in. A µbench with ONE launch per eval measures the BARRIER (0.14 ms).
- **A verify lane is never byte-identical to stock, and a parity bar never references stock's WORST element — cosine cannot stand in for per-element** (`VerifyQmmParity`, `MLX_SERVE_VQMM_PARITY_DEBUG=1`): fp32-dequant parity per width, bit-identity of 4-bit ACROSS a refactor, gross ceiling 0.5, RMS ratio vs stock 3.0x. Reverse CELL order per pass.
- **verifyQmm lanes** (`vqmmLaneFor`): split-K M 2–7 / wide tile N≥100K / NAX m16 M 8–16 (q8: M 7–16; G17) / crossrow OPT-IN (`MLX_SERVE_VERIFY_QMM_CROSSROW=1`). NAX mixed-width q5/q6/q8 g64 N≥5120 (`MLX_SERVE_VERIFY_QMM_NAX_MIXED=0`); q8 needs its own unpack arm. Codegen NAMED SCALARS; M=8 is the plain-SIMD cliff.
- **EV cost tables are refit whenever the verify forward changes** (`MTP_EV_DEFAULT_COSTS` carries method + numbers; `MLX_SERVE_MTP_EV_COSTS` ZEROES the third region), and an MTP sidecar never NAMES the target surface: `MtpCostProfile` comes from the full runtime fingerprint, unmeasured = generic/cap-6.
- **A qwen4 draft step was BYTES (the 8-bit 675 MB lm_head), not dispatch latency**: greedy drafts shortlist on a coarse copy and re-score exactly (`buildRerankCoarse`/`rerankSelect`/`fullReadoutArgmax`) from the MIXER output (`StepWant.mixed` → `rerank_x`), NEVER the pre-mixer stream; `MLX_SERVE_MTP_DRAFT_RERANK=0` restores the full readout.
- **qwen4 has its own measured G17 surface** (`g17_nax_qwen4_q4_gs64`): a verify row is bytes at EVERY width (routed experts never ride the vqmm lane), so its cap KEEPS the default 6; `MLX_SERVE_MTP_QWEN4_PROFILE=0` revokes the profile ONLY. Its `draft` term predates the rerank — owes a refit.
- **A cost-profile A/B with the persisted round-cost table live measures the TABLE, not the profile** (`MtpCostSource` plans from a matured bucket): `MLX_SERVE_ROUND_COST_PERSIST=0` on BOTH arms, within-boot serial cell = drift meter, boot order counterbalanced (same-evening boots drift 5-12%).
- **The grouped-expert NAX tile is a measured LOSS at verify widths** (~−9%: 1-2-row runs never amortize m16; SORTED gather_qmm is near the expert-byte floor); kernel + parity test parked at `holtsway/mlx-serve@feat/qwen4-moe-verify-nax`.
- **The qwen4 MTP head projects only the row its caller consumes** (`Qwen4MtpProject`): slice `h` BEFORE the mixer + lm_head, skip both for a history append, or stop at the mixer for a rerank draft. Drafts steer ACCEPTANCE, never bytes, so the bar past S=1 is fixture cos, not bit identity (M=1 and M=S are different kernels).
- **The qwen4 EV seed lives on `Qwen4Mtp`** (`ev_seed_accept`/`ev_seed_m_lo`, twin of `MtpModel`'s): per LOADED MODEL, dies with the head. Publish AND consume decline under `MLX_SERVE_MTP_FORCE_DEPTH` (never plans, must neither inherit nor poison). A code-trained seed costs the first prose request ~7 tok/s.
- **Auto-mode MTP output is NOT byte-reproducible, so byte identity is no A/B bar there**: round times pick depths → widths → kernels → greedy near-tie flips; the CONTROL differs from ITSELF across boots (4/9 cells). Byte bar = `MLX_SERVE_MTP_FORCE_DEPTH`; in auto only the first request and spec-off cells are stable.
- **Reproducing an upstream NO-OP faithfully means NOT doing it** (Kokoro SineGen): measure the reference's own seed-to-seed self-similarity first.
- **Kokoro traps**: safetensors READ on CPU stream; depthwise `ConvTranspose1d` transposes `{0,2,1}`; complex via `mlx_array_new_complex`; `AdaIN1d` is pure instance norm; vocab has uppercase ASCII as diphthongs, every emitted symbol must be IN it (`Vocab.encode` DROPS unknowns).
- **Kernel testing**: GPU parity = no-worse-than fp32 ground truth, never kernel-vs-kernel; same-boot A/Bs at per-cell MEDIANS; every shape an eligibility predicate adopts gets its own A/B; a big forward fix invalidates every ratio calibrated against the slow one. CONTENT-forking arms need 3+ REPS, NONCE per REP never per arm.
- **A sparse-attention PATTERN that isolates a spatial neighbourhood cannot be tuned into correctness** (H3, DEAD): CLIPS decide; H3 DiT is at roofline — wall clock moves only by FEWER forwards.
- **MoE PREFILL without the global sort re-reads each expert bank per TOKEN**: new MoE prefill uses `_gather_sort` (decode C==1 unsorted). **dsv4 decode glue is FUSED** (`dsv4_emit_win`, `dsv4_dec_chain` +8.6%; exact `frexp`/`ldexp`).
- **A cache that hands out BORROWED handles evicts LRU, never a fixed slot** (`vqmmScalarEvictIndex`). Whole conformance surfaces 0/N = DEAD PROCESS, not a bad model.
- **A `metal_kernel` helper owes its own `streamIsGpu` guard; a per-token-varying TEMPLATE value is a fresh JIT per value** — ramping values ride INPUTS.
- **dsv4 comp_in int8 requant is EXPLICIT-opt-in** (`decodeAttnQuantExplicit`) — a shared lossy flag's default is not transferable between archs. Stock `gather_qmm` is the default outside the qwen4 decode kernels.
- **A kernel's dtype and its threadgroup BLOCK SIZE are ONE decision** (`gdnBlockTFor`; dtypes off the ARRAY; Metal has no implicit float→bfloat).
- **Every KV buffer is sized from the OPERAND it stores; a scheme with a shape constraint refuses at LOAD**: K/V widths differ on MLA; TurboQuant needs pow2 → `initWithConfigAndHeadDim` checks `kvCacheKeyHeadDim()`.
- **A fallible re-init BEHIND a `deinit` leaves a freed object on the error path** — build first, then swap (`KVCache.reinit`). Scan-pinned: no `.cache = try` in transformer/scheduler/main; the fused-kernel config caches carry the same rule (guard: "fused-kernel config caches are built BEFORE the previous handle is released").
- **A handle freed before a fallible op is reset AT the free** (`updateDense` views, as `updateAffine` does): a write that failed after the free left freed views in the entry for the next `resetCache` to free again (SIGSEGV in `freeKVEntry`). Guard: the `KVCache dense update` fault sweep.
- **An MLA cache is billed per ATTENTION head** (`kvBytesPerToken`): the latent decompresses to all heads; only an asymmetric config tells the spellings apart.
- **A gate's LOWER BOUND may REPLACE the formula, not clamp it** (KDA: `g = exp(bound·σ(…))`); two arms means the arm is SELECTED (`kdaUsesBoundedGate`): absent bound = softplus arm, never bound 0. Read the reference KERNEL's arms.
- **Per-head vs per-channel gating is an INDEXING contract**: one recurrence generated for both (`gdnKernelSource(vectorized, capture_seq)`); other-shape kernels DECLINE; test = per-channel gate held UNIFORM must match the scalar kernel EXACTLY.
- **A struct inside a generic fn that captures NO comptime param is memoized to ONE type** — capture the params (`const is_vectorized = vectorized;`).
- **A custom kernel's signature comes from each input's ACTUAL dtype; <8-element arrays land in `constant`**: index raw buffer names; every new kernel ships a one-shot "engaged" log + parity on the LIVE dtype.
- **A helper that materializes a VIEW owns the view** (`sliceContig`, one per file). RSS is blind to Metal — `/props` `active_bytes` + vmmap. **A weights MAP outliving the model pins every buffer**: scope inside `Model.load`. **mlx-c iterator refs**: `iterator_next` hands a +1.
- **Allocator-cache growth**: `mlx_clear_cache()` once per CHUNK and per emitted block; the clear must be un-skippable (#110): `server.applyMlxCacheLimit` ONCE at top of `main()`; cadence INTERVAL-based (`step -| last_clear >= 256`); `Generator.advanceStep` is the one step mover (scan-pinned); `finishSlot` clears the tail.
- **KV growth is PROPORTIONAL** (`nextCapacity` +25%, floored one chunk, capped 8192). **`active` flat while phys climbs = the POOL**: `/props` `memory.cache_bytes`, `mlx_serve:mlx_cache_bytes`, tray `(+N GB cache)`.
- **Special-token splitting only in `Tokenizer.encode`** (first-byte buckets) — per-special rescan is O(specials×text), shipped TWICE. Any per-position loop over a vocab-derived collection needs an index.
- **mlx `Copy`/`contiguous` are VIEW ops**: a slice OUTLIVING its parent goes through `materializedOwnedCopy` + eval. `mlx_save_safetensors` appends ".safetensors". **A raw data-pointer read must PROVE row-major contiguity** (post-eval strides; FLAT RESHAPE).
- **mlx-c upgrades**: diff `lib/mlxc-src/mlx/c/*.h` vs `src/mlx.zig`. ds4 bumps: re-sync `ds4_ffi.zig` EngineOptions + mirror CORE_OBJS in build.zig. **NAX presence asserted, never assumed.** **`mlx_array_new_data` COPIES shape-worth of bytes** — a smaller buffer is latent UB.
- **A model whose per-request state lives OUTSIDE the KVCache is excluded from prefix-cache reuse AND owed a `Transformer.deinit` arm** (dsv4 leaked 108 GB). **`openDirAbsolute` on an empty/relative path is ReleaseFast UB that miscompiles the CALLER** — guard every site.
- **ANE prefill (opt-in, M4-and-below, LOSSY)**: full rule set lives in `docs/reference.md` "ANE prefill rules" (staging, plane pitch, tile sizing, channel split, int8 only, procedure banks, staging reaper). Guard: `tests/test_ane_prefill.sh`.
- **ANE parity bar is per-program cos/rms + perceived-content greedy equivalence, never byte-identity** (fp16 planes are 1 ulp from f32); a full compile cache or disk ships SILENT PARTIAL COVERAGE (`ready: N/M` under-count is the tell); a silently ignored affinity hint is undetectable in-process (`macpow --dump | grep ANE0_`).

### Model loading, configs, converters, media parity (→ docs/gotchas/models-media.md)

- **A qwen4_exp checkpoint is NOT a qwen3_5 pack** (model_type `qwen4_exp`): three new blocks (hyper-connections, n-gram PLE, QSA) around the qwen3_5 trunk. The HF fixture's `hidden_states[i]` is the INPUT of layer i (stream_0 = tiled embeddings) — compare layer-i output with stream_{i+1}.
- **A random tiny MoE oracle ties everywhere, and the tie RATE is scale-invariant** (relu leaves EXACT-ZERO block scores; bf16 noise flips one top-k expert or QSA block per prompt): `dump_qwen4_exp_fixtures.py` runs every expert and dumps the reference's OWN margins; `Qwen4Ties` acquits under 3% by those, NEVER by our output.
- **A k < E tiny fixture ties in most trunk rows across every MoE layer, so its top-k SELECTION coverage is the MTP head's ONE MoE layer** (`dump_qwen4_exp_fixtures.py build --topk 2`; `route_gap`/`mtp_route_gap` = softmax margin at the top-k boundary, `QWEN4_TIE_REL_ROUTE` 5%). The k = E fixture stays the CI one.
- **qwen4_exp splices vision rows BEFORE the hyper-connection tile** (`forwardQwen4With`: `applyVisionEmbeddingsWith` on the `hidden`-wide embeddings, THEN `mlx_tile`). Tower + preprocessor + M-RoPE table are Qwen3-VL's (`config.qwen_vision`, shared with qwen3_5); the pack gets the tower via `--add-vision`. Guard: `qwen4 fixture vision`.
- **QSA's always-visible tail is PER QUERY** (tokens at/after `ratio·floor((p+1)/ratio)`), never `kv % ratio`; scores in f32 like the reference; `torch.topk` keeps the LOWER block index on exact-zero ties. The n-gram hash's eos is the TEXT config's (`ngram_eos`), not the generation stop set.
- **On qwen4_exp the spec "hidden" IS the pre-mixer stream** (`capture_hidden(_all)` = `[B,L,hc*hidden]`, never the mixed 2560): the MTP head fuses `fc_hidden(rms_{4H}(stream)) + fc_embedding(rms_H(embed))`. Head row r = (stream at r, token r+1) at query position r+1 — QSA takes a `pos_base` because its cache starts at position 1.
- **Per-request state outside conv/ssm rides `SSMCacheEntry.aux_state` + `ple_prev`** (PLE conv window, QSA raw-key history + its capacity buffers); every reset/free site goes through `ssmFreeQsaState`. `SSMCacheEntrySnapshot` carries them, so the prefix cache is ON. Restore-parity bar = the CHUNKING class (~0.3 nats top-5), not 0.047.
- **ACE-Step's timbre slot is the silence latent OR the reference clip's VAE mean, never both** (`MusicRequest.ref_audio`, #259): a conditioning SWAP, not a branch — `acestep.referenceWindow` makes every clip 30 s and `refTimbreLatents` encodes the MEAN. Guards assert the `[acestep] reference audio:` line, never output.
- **An ACE-Step TASK is the DiT's context stream plus the instruction line — same forward** (`MusicRequest.task`): `complete` (vocal2bgm) = source clip's raw VAE latent as `[src|ones]` + its line; `cover` = latent → pooler → FSQ (soft clamp `c·tanh(z/c)`, THEN the hard-clamp grid — neither alone) → detokenizer; source length IS track length. FSQ weights ship SEPARATE, DENSE bf16 (`fsq.safetensors`).
- **A standalone-frame latent belongs in latent slot 0 ONLY** (#260: elsewhere a 1-frame latent decodes as 8 frames of noise). First anchor REPLACES frame 0 (`VideoConditionByLatentIndex`); last anchor is APPENDED as `HW` clean tokens at pixel `num_frames-1` (`VideoConditionByKeyframeIndex`), trimmed before decode. `keyframeMask` + `keyframePositions` are the ONE place.
- **A capability the reference API exposes as a FLAG may reach the checkpoint only as TEXT** (`instrumental`, both music backends → `[Instrumental]` section tag). Flag + non-empty lyrics = NAMED 400 (`gen.instrumentalConflicts`; whitespace-only = absent). Guards assert `instrumental=true` IN THE LOG.
- **An adapter's alpha lives wherever its exporter put it** (`lora.fileAlphaScale`: nested-PEFT > flat > `ss_*` > none; per-module tensor WINS; non-finite DECLINED). A LoRA test comparing only BYTES can't tell adapter from noise — `tests/lora_noise.py` + `tests/test_real_loras.sh`.
- **A latent→RGB preview is a FIT per latent SPACE; the oracle proves fidelity to the REFERENCE, never resemblance to the video** (#208): `src/latent_rgb.zig` is GENERATED by `tests/dump_latent_rgb_factors.py` executing the reference (GPL spec, never transcribed). Live bar VAE-decodes a real latent and correlates against `goldenAngleControlMap`. `LTXAV` ≠ `LTXV`.
- **A condition stream is assembled in SEGMENT order by ONE resolver** (H3 `resolveRefs`; `refBlockFor` DERIVED). **Two partitions with IDENTICAL files need a DECLARED discriminator** (H3 `tasks` → `h3ConfigDeclaresRef2va`; server 400s, app gates the FIELDS).
- **A tiled decoder's tiles are not slices of an untiled pass when positions normalize over the EXTENT**: `fitsSingleTile` → `error.TilingUnsupported`; temporal chunking is semantic (17-frame clips).
- **A COSINE parity test cannot see a SCALE error**: anything CONCATENATED asserts `rms_ratio` too. A faithful port can make the checkpoint WORSE (H3 `<Picture i>` splice default-off fl2va). **An oracle that cannot execute the reference must say so.** **A permutation-invariant checksum cannot see a permutation** (`pos_weighted`).
- **A distilled FEW-STEP diffusion model can REQUIRE bf16** (MageFlow Turbo): RoPE rotates f32 then casts; timestep + freq table bf16-ROUNDED (`roundBf16`); `scalarLike`. Debug the LOOP per-step in the native dtype.
- **A reference-image editor must not change its input's geometry**: `resolveEditTargetSize` = `fitAspect` → `fitWithinCap`; `encodeEdit` asserts pad count == merged vision rows.
- **A gather-read table may be quantized only where the READER has a quantized-gather path**: media backends read with dense `mlx_take_axis` → `NEVER_QUANTIZE` lists; LM `embed_tokens` via `gatherQuantizedRows` is a SIZE decision (measured within ±2se; our packs quantize it, `tests/restandardize_qwen38_pack.py`).
- **Publish MTP head norms FOLDED** (`--fold-mtp-norms`): same bytes at serve time as delta + load-fold; drops reliance on detection heuristics. A pack whose BACKBONE norms move the anchor (Alis) is what breaks oMLX, not the convention.
- **A chain that returns f32 BY DESIGN makes the CALLER own the activation dtype** (`groupLimitedRouting`): forgetting promotes the residual at the first MoE layer — ONE `[dtype-trace] residual widened` line is the symptom.
- **Calibrated quantization**: channel weights PER-INPUT-CHANNEL and PER-EXPERT (imatrix value IS the weight); bit width beats group granularity ≤3 bits; round (s,b) to the STORED dtype before q; self-test against a weight-BLIND arm.
- **A sampler never draws a RESERVED special OR a PADDING row; the legit set is DERIVED** (`tokenizer.reservedOutputIds` + `definedVocabSize` → `installSuppressMask`, `MLX_SERVE_SUPPRESS_RESERVED=0`): `[defined, logits_dim)` decodes to nothing and is masked, unless the checkpoint declares `unpadded_vocab_size` (ONE trim, never both). Logprobs stay RAW.
- **A hand-rolled pretokenizer is calibrated to ONE tokenizer.json — digit GROUPING is per-model** (`Tokenizer.digit_group`); mis-grouping presents as a QUALITY bug; cross-check `/tokenize` vs HF at bring-up. **A COMBINED Split regex hides the digit rule** (Llama-3 → `pretok_style = .llama3`).
- **The degenerate-tail loop guard needs a LONG-period tier**: periods 9..64 at 10 reps (`isDegenerateTailLoopRange`).
- **An imatrix is only valid for the WEIGHTS it was collected on**; byte-identical shard reuse is enforced by COPYING. **Uniform ≤2-bit experts to the LAST layer cause TURN-LEVEL agent loops** — low-bit gates need an AGENT-LOOP cell; 4-bit tail experts fix it.
- **A configless repo SHAPE must be taught to every "is this a model?" answer**: ONE predicate (`model_discovery.peekMageFlowIndex`; klein keyed on a WEIGHT NAME); `gen.peekModelType` DELEGATES.
- **A directory filter that skips `.sym_link` measures an HF-cache model at ZERO**: every size sum stats THROUGH symlinks (`statFile` follows, post-stat `.file` check). Sites: `scheduler.modelDiskBytes`, discovery, `gen.sumSafetensorsIn`, cli.
- **An incomplete media pack is invisible until its marker lands** (`model_discovery.requiredMediaMarker`, `gen.requiredMarkerFor` delegates): discovery skips, `probeModelDir` refuses `error.IncompleteMediaPack`, preload refuses by name. Guards: `tests/test_model_rescan.sh`, `TurboLoraFetchTargetTests`.
- **A family's geometry comes from the CHECKPOINT once a second size exists** (FLUX.2 klein 4B/9B): derive from SHAPES; TE heads from `q_proj` ROWS; fused gate+up = rows÷2.
- **An engine that hardcodes its pack's quant WIDTH can only load that pack** (ltx `bits=4` literal killed the 8-bit pack): every site solves via `affineParamsFromGeometry` (`quantGeom`). Scan-pinned: no literal width at any `mlx_quantized_matmul`/`mlx_dequantize` site.
- **4-bit affine on a video DiT is a QUALITY setting** (LTX-2.5: ~9.9% noise/linear vs 0.6% at 8-bit, 3.5% time cost). Mirror `ddalcu/LTX-2.5-MLX-Serve-8bit`; `scripts/quantize_ltx25.py --bits 8`.
- **A module's constructor arguments are not its config** (LTX DiffVAE: class defaults run the weights into STATIC; measured 1-step x0 at t×1000). `ltx_diffvae_forward.Sampler` pins all three behind kill switches; faithful-port parity CANNOT see this.
- **The DiffVAE's tile budget is a per-REQUEST memory decision** (`tileTokensForMemory` from free RAM at decode; stages 1-3 full volume, stage 4 + diffusion tile with trapezoid ramps summing to 1). Not a load-gate term.
- **A canvas the model was never asked to fill cannot be sharpened by steps/guidance/quant**: `VideoModelPreset.recommendedResolution(totalGB:)` sizes per Mac, capped by `autoCanvasCapPixels` (TIME budget); two-stage denoises at HALF (`twoStageCanvasNote`); frame ladder per canvas; `maxFramePayloadBytes` is a TRANSPORT limit.
- **A media pack's RELEASE is a config field, and the text encoder is what it decides** (`LtxVersion` from `model_version`, absent ⇒ 2.3): 2.3 borrows `mlx-community/gemma-3-12b-it-4bit`, 2.5 ships `gemma4-12b-ltx-v1/` in-pack (`MediaBundle.ltx25TextEncoderDir`). A mismatch renders a plausible video for the wrong prompt.
- **The 2.5 text encoder runs the REAL gemma4 transformer** (`ltx_video.gemmaCapture4` via `capture_layers` + `skip_lm_head`); left-padded prompts ride `ForwardCtx.prefill_mask_add` (already causal, FINITE -1e9, declines both fused prefill kernels; null on serving paths).
- **`keyframes_abs_pos_embedding` marks GENERATED keyframe slots, never given-content tokens** (reference `extend_keyframes_mask(marked=False)` for image guidance; the 2.5 checkpoint's row is ~0 anyway). We build no generated slots, so the parameter is parsed and never added.
- **A bytes-read table prices a lever only for a path already AT its read floor** (music3 depth KV: estimated 1–3 ms, measured −11.6). Per-stage probe laps (`MUSIC3_COST_PROBE`) before bandwidth arithmetic.
- **Music3 is NOT an ACE-Step variant**: timestep is a prepended TOKEN (no AdaLN), SwiGLU REVERSED (`first * silu(second)`), Snake alpha-only no exp, DiT RoPE dims-32 theta-10000 hardcoded, AR frame 0 never emitted but feeds back. Full list: the music3 story.
- **When an arch's reference IGNORES a config field, that field is not the truth** (laguna YaRN mscale COMPUTED). **A config field HF allows in two SHAPES must be read as both** (`chat_template` string OR list); `.string` on unchecked `std.json.Value` panics. **Read `text_config` FIRST, then root, PER FIELD.**
- **`x-mlx-pixels` is a GEMMA format; the client gate is an allowlist of ONE** (`wantsServerPreprocess` = `!hasPrefix("gemma")`); server `parseImageUrlContent` refuses when `vp.mode != .gemma`; `VisionEncoder.forward` returns named errors, never dereferences sentinels.
- **The text-model loader's `--no-vision` drop list is a GLOBAL prefix filter, so a media backend that shares a key spelling with an LM tower loses its weights** (MageFlow Edit's `model.visual.*` vanished the day the Alis-pack rename joined the list; every Edit load 503'd `MissingMageFlowWeight` for weeks). A tower loads through `loadWeightsWithVision`, never `loadWeights`. Guard: `VisionTower.openWeights` test in `mage_flow.zig`.
- **A placeholder id the splice mask does not name is a modality that never reaches the prompt** (`spliceVisionRows` keyed on image | audio ids; Qwen VIDEO pads were inserted, encoded and never scattered). Mask + `countSpliceRows` take `video_token_id`; an e2e vision bar must name something only the pixels could supply.
- **A chunked vision splice resumes its ROW INDEX** (#197, `vision_splice_offset`); repro needs prompt > chunk+512 (`TAIL_MERGE_MAX`); bar = PERCEIVED-content invariant, never bytes. Guard: `tests/test_vision_chunked_prefill.sh`.
- **A forward ARM that never splices vision is invisible from the outside** (LFM2-VL hybrid arm answered "a black rectangle"). Class guard: source scan `every generative forward arm splices vision embeddings`, encoder-only arms exempt BY NAME.
- **A resampled position table is `bilinear` WITH antialias** (SigLIP2-NaFlex; mlx-vlm's bicubic is a 1.13x rms scale error, not an oracle): `qwen_vision.resampleWeightMatrix(.bilinear)`.
- **Patch feature order is per-ARCH** (LFM2-VL channel INNERMOST; Qwen/Muse outermost). **Two GELUs in one checkpoint** (LFM2-VL encoder tanh, projector erf). **For NaFlex, TILING is the resolution** (canvas resampled ONCE, read as windows). Guards: `tests/test_lfm2_vision.sh`.
- **A checkpoint's processor pixel bound is not the ENGINE's** (`qwen_vision.effectivePixelBounds`, `ENGINE_MAX_PIXELS` 1536²): our tower MATERIALIZES the score matrix; a 5100x3300 photo OOM'd. Logs once.
- **A tower's PREFIX and `patch_embed.proj` AXIS ORDER are converter choices — probe** (`resolveVisionPrefix` + `patchProjLayout`; channels-last vs channels-first reads as a striped-pattern tower). Probe colours in THREE.
- **Two towers on one surface means the PREPROCESSOR is part of the arch** (muse: aspect-closest grid under MERGED cap, Lanczos, row-major, CHANNEL-major shuffle). `VisionPreproc.mode` picks all of it. Guards: `tests/dump_muse_vision_fixture.py`, `tests/test_muse_vision.sh`.
- **A config default only ONE family wants is a per-arch trap — pin every value PER LAYER TYPE** (muse sliding layers read `rope_local_base_freq` default 10000 vs its 500000 → first-turn loops). Guard: `tests/test_muse_repetition.sh`.
- **A module TREE is the converter's choice** (`lagunaRouterBase`: flat `mlp.gate.weight` vs nested `.proj.`; probe, flat preferred — the `hy3ExpertContainer` pattern). **WEIGHT PREFIX is a property of the checkpoint** (`model.resolveWeightPrefix` probes).
- **A layer-init path that DEMANDS `.scales` can't load a DENSE checkpoint**: scales absence PER-TENSOR (`getLayerScaleOpt`); every dense contracted weight owes `maybeTransposeForBf16` — never depthwise conv or SSM state. Source scan rejects the mandatory getter.
- **`*_text` siblings**: accept the tag, collapse to base type, prefix by `text_config` presence, force `tie_word_embeddings` for Gemma, add to BOTH visibility allowlists. **Gemma terminators merge ADDITIVELY** (`ensureGemmaTerminators`).
- **Quant modes resolve PER WEIGHT** (`computeQuantParams`; scales dtype decides fp8 vs affine; `.biases` mandatory under affine). **A per-tensor override can hide INSIDE the fp family** (`fpParamsFromGeometry` solves from packed geometry). **Affine bits outside {2,3,4,5,6,8} rejected at PARSE** (`UnsupportedQuantBits`).
- **A GGUF folder is a SHELF of models**: one `LocalModel` per quant (`id#<file>`), sidecar classifiers in sync in Swift AND Zig.
- **DiffusionGemma parity = converged-canvas self-consistency** (`MLX_SERVE_DIFFUSION_TRACE=1`); sliding prefill takes `"causal"` when `total_kv ≤ sw`. **Optional per-token modulation gates on the mask** (`nv_pt==0` → exact scalar path).
- **Parity fixtures for deep ViTs/DiTs: dump fp32 on CPU** (MPS fp16 decorrelates); transformers ≥5 zeroes custom rotary buffers — dump scripts assert non-zero; an oracle that IMPROVES when you remove a transform had it silently disabled.
- **TTS**: `TimeDelayNetBlock` = conv + IMPLICIT ReLU; embedding lookups reshape to the TABLE's width (`getShape`); voice clone = ECAPA embedding as one codec-prefix position, `ref_text` ignored.
- **A pinned library's op TRANSIENT is invisible to every residency bill** (#321): MLX ≥ 0.32.2 splits a temporal-pad-0 N=1 3D conv into per-tap 2D convs under a whole-GPU Winograd budget; LTX VAE decode OOM'd. `conv3dDepthChunked` windows decoder convs (exact). Diff conv DISPATCH per mlx bump.

### Licensing & third-party code

Ported kernels + vendored code are enumerated in `NOTICE` (the ONE place); `LICENSE`/`LICENSE-APACHE-2.0`/`NOTICE` ride every packaging path, pinned by `tests/test_release_workflow_gates.sh`. To enumerate ports, grep comments for `mlxfast|oMLX|MTPLX|mlx-lm|port`. Full story: `docs/reference.md` "Licensing".

### App (Swift)

All app-side rules: `app/CLAUDE.md`; stories: `docs/gotchas/app.md`. Crossovers: agent budgets derive from advertised `context_length`; the app ALWAYS emits memory-critical launch flags (`ServerOptions` defaults mirror Zig defaults — change both together); the two bundle binaries move together.
