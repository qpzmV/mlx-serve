# 2026-08-31

## expert-streaming-handoff.md 审读（用户要求评价）

- 抽查 13 个行号锚点全部命中（moeMLP2@17115、shouldKeepWeightKey@3466、SlotPool@ExpertStore.swift:125、MoELayer.callAsFunction@Layers.swift:385、F_NOCACHE@Checkpoint.swift:257 等）。
- **发现 P0 风险**：文档 Step 1 说池散射"用 mlx_slice_update 或 mlx_take_axis"，但 MLX 的 slice_update/scatter 是函数式（返回新数组、整池拷贝），16GB 池每次填 48 专家会拷贝整池；slotstream 实测散射只写 132MB@74GB/s，说明 Swift 侧做到了等效就地写，但文档没交代用的什么原语。这是移植生死点，开工前必须先解决（mlx_array_data_ptr 直接写 buffer，或核对 slotstream 实际散射实现）。
- P1：文档完全没提与 mem_pressure 看门狗的交互（池 16GB 预分配 + ngram 29.8GB 页缓存 + KV 可能触发 evict/exit）。
- P2：踩坑 7 只讨论 S 未讨论 B（batching 下 total_inds=B*topK，B≥7 强制走排序路径）；slotstream 是单请求 CLI，多请求并发时池 pin 状态/线程安全未提。
- 小问题：Step 3 给了"最干净/更稳妥"两个方案没拍板；验收标准 12-20 tok/s 是 slotstream 48GB 机参考值，写成验收指标易误导。

## scatter 就地写验证（用户确认后执行）

- **结论：就地写成立，但依赖「唯一引用」前提。** 完整证据链：
  1. Swift setter（MLXArray+Indexing.swift:376-398）落到 `mlx_scatter` 产新数组 + `mlx_array_set` 换句柄；
  2. `Scatter::eval_gpu` 第一步 "Copy src into out"（indexing.cpp:248-257），看似整池拷贝；
  3. 但 `copy_gpu`（metal/copy.cpp:13-19）先走 `set_copy_output_data`（common/copy.h:25-48）：`is_donatable(in,out)` 为真时 `out.copy_shared_buffer(in)` 零分配接管 buffer，且 `donated && 同 dtype → return` **跳过整个拷贝**，scatter 内核只写 upd.size() 触及元素；
  4. `is_donatable()`（array.h:294）= `array_desc_.use_count()==1 && data.use_count()==1`。
- **实测**（mlx 0.32.0，2048 槽 5.66GB 池，48 专家 batch）：整池拷贝基线 21.41ms；batch scatter **1.13ms**（18.9×）；散射期新增分配 peak=0.00GB；写入正确。slice_update 0.72ms 同走捐赠路径。
- **移植纪律**：池数组必须全程只被一个句柄持有；若 mlx.zig 包装是 retain 语义，`MoeMlpWeights.switch_*` 绑池块会 +1 引用导致捐赠失效、**静默**退化为整池拷贝（27GB 池 ≈100ms/次，decode 废掉）。哨兵测试：2048 槽散射 48 专家断言 <5ms。golden equivalence 抓不住此 bug（只慢不错）。
- **发现：docs/expert-streaming-handoff.md 已被删除**（docs/ 目录中已不存在，验证结论无处落盘，已口头汇报）。
- **文档去向**：用户把 handoff 文档移到 `/Users/robin/workspace/1@inference_engine/expert-streaming-handoff.md`（移出 docs/，git 未跟踪）。已把验证结论补进文档：Step 1 条 4（散射原语改为 mlx_scatter+mlx_array_set / mlx_slice_update，注明捐赠机制）、踩坑 13（捐赠依赖唯一引用 + 移植纪律 + 哨兵测试）、验收标准新增第 4 条（散射哨兵 <5ms）。
- 实验脚本：/tmp/scatter_verdict.py。
